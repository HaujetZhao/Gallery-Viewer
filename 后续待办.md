# 后续待办（跨阶段遗留事项）

> 记录每个阶段发现、但需延续到后续阶段才处理的事项，避免遗忘。
> 格式：`[来源阶段]` 事项 · 影响 · 何时处理 · 状态

---

## 阶段 0（工程骨架）

- **单 HTML build 的 dist 含多余图标文件**：`public/` 下的 `icon.svg` / `icon-192.png` / `icon-512.png` 会被 Vite 原样拷到 `dist/`，导致单 HTML build 的 dist 不是「纯单文件」。
  - 影响：无（`index.html` 自包含 JS/CSS/字体，可单独拷走双击运行；多余图标无害）。
  - 何时处理：可选优化，优先级低。若要「真单文件」，需在 `vite.config.js` 处理 `publicDir`，但 PWA build 又需要这些图标，两套 build 冲突，得不偿失。
  - 状态：**暂不处理**（SubTap 同样行为，可接受）。

## 阶段 1（配置基础）

- **主题变量残留**：三套主题 CSS 变量键不一致（ocean 最全；dark 多 `--color-success/-warning/-danger` + `--text-inverse`；forest 无 gray 系列）。`applyTheme` 只 `setProperty` 不 `removeProperty`，切到变量更少的主题时上一个主题的值会残留。
  - 影响：`dark → forest` 切换后，forest 下用到 gray / 语义色的 UI 可能显示 dark 的深色值（观感异常）。
  - 何时处理：阶段 5（画廊卡片用 gray）/ 阶段 9（属性面板用语义色）实际暴露时评估。
  - ✅ **已处理(2026-07-27)**：`applyTheme` 开头先对「三套主题变量键并集」逐个 `removeProperty`，再设新主题值。未覆盖键回退 variables.css `:root` 默认(ocean 风格浅色,forest 合理)，不残留上个主题的深色值。
  - **未用 `cssText=''`**：会误清 `--sidebar-width`(useSidebar 设的)，导致切主题时侧栏宽度重置；故精准只清主题键（`src/stores/theme.js`）。

## 阶段9（属性面板）

- **单媒体卡片显小**：文件夹只有 1 个媒体时，卡片在 masonry 第 0 列（1/colCount 宽），右侧空白多显得孤立/小。源码 `gallery.js` 也是 `index % colCount` 同逻辑（非迁移 bug，单卡片和多卡片卡片实际宽度相同）。
  - 影响：纯视觉（单卡片孤立感），功能正常。
  - 何时处理：可选 UI 改善。方案：卡片总数 < colCount 时 gallery-grid 居中，或单卡片放大。
  - ✅ **已处理(2026-07-27)**：根因是 `body { display: flex }`，但 body 唯一直接子元素是 `#app`(无 flex:1/width:100%) → 按内容收缩；`.main-content-wrapper` 的 flex:1 是 body 孙级，对 body flex 无效(flex 链在 #app 处已断)。删 `body` 的 `display:flex` 后，`#app` 作为 block 子元素默认 100% 宽，内容不再被压窄（`src/styles/base.css`）。
  - 用户补充：经查 card 变小是 app 变窄导致。在 F12 中将 body 的 flex 取消后，变正常。← 此线索直接定位根因。

## 阶段10（音频播放器）

- **音量滑块无法拖动/点击**：根因是 `useModal.onMouseDown` 的 `e.preventDefault()`（绑在 modalEl，事件冒泡）对 AudioPlayer 内 input/button 的 mousedown 也生效，阻止了音量滑块原生拖动。音频 modal 本身不能像图片/视频那样拖动位置，但拖拽逻辑仍拦截了内部交互。
  - 影响：音量滑块拖不动/点不动（可能也影响进度条 handle、按钮的 mousedown 默认行为）。
  - ✅ **已处理(2026-07-27)**：`onMouseDown` 开头加 `e.target.closest('.modal-audio-player, input, button, select, textarea')`，命中则 `return`(不 preventDefault、不 panning)，放行音频播放器内滑块/按钮及通用交互控件的原生行为；图片/视频拖拽不受影响（`src/composables/useModal.js`）。

## 架构重构（资源层分离 + 纯 model，Phase 1-4，2026-07-28）

- **[Phase 2 验收] 切文件夹后内存增长 ~4MB**：可疑泄漏。根因：`switchToRoot` / `initProject` 切换/重载时 `fs.foldersData.clear()` 只清 Map，**不 dispose 旧 SmartFile** → Phase 1 池化后旧文件的 blobUrl/File 条目驻留 `fileResource` 池不释放（revoke 缺失）。部分增长也可能是正常缩略图位图（新文件夹解码）+ GC 延迟，需 DevTools 确认。
  - 影响：反复切换文件夹内存单调增长（用户实测 266↔4 文件夹切换 +~4MB）。
  - 何时处理：Phase 3（动 foldersData 注册时顺手）或专门修。修复 = clear 前遍历旧 foldersData 调 `folder.dispose()`（→ file.dispose() → destroy 池条目）。排查 = DevTools Memory 对比切换前后 blob URL/File 数。
  - ✅ **已处理(2026-07-28)**：`filesystem.js` 提取 `resetFoldersData`,`initProject`/`switchToRoot` 的 `clear()` 前遍历旧 foldersData 调 `folder.dispose()`(→ `file.dispose()` → `destroy` 池条目)。dispose 幂等(ALL_MEDIA 聚合引用与真实 folder 共享 file,重复 destroy 无害)。待用户 DevTools 验收内存不再单调增长。

- **[Phase 2 验收] 列布局：切排序后列高度不对齐 + 上滚时列纵向位置跳**。根因：`.photo-card` 用 `content-visibility:auto` + `contain-intrinsic-size:var(--estimated-height)`（[gallery.css:53-55](src/styles/gallery.css#L53)，大文件夹性能优化：屏幕外卡片跳过渲染用占位高度），但占位高度 `estHeight=(innerWidth-300)/colCount+60` 估算不准（没算 gap、信息区高度浮动）→ 滚动时屏幕外卡片进视口，高度从占位跳到实际 → 列高度跳变 → 下方上移。Phase 2 的 enrich 逐个写 `_meta` 触发 N 次 sort 重排，叠加加剧切排序时的列重算。
  - 影响：切排序/滚动时列高度与纵向位置跳变（纯视觉，功能正常）。
  - 何时处理：Phase 3。方向：① I3 批处理（requestAnimationFrame 合并 _meta 写，减 sort 重排频率）；② estHeight 算准（加 gap + 信息区固定高度/filename 截断不换行）；③ 或 Gallery 虚拟化替代 content-visibility（精确高度）。注：`content-visibility` 是预存 CSS 优化（非 Phase 2 引入），Phase 2 重排叠加。
  - 状态：**待办**。

## 通用

- **npm 传递依赖漏洞**：`npm install` 报 10 个漏洞（2 moderate, 8 high），来自依赖树传递依赖。
  - 影响：本应用纯本地文件处理、不联网，实际风险低。
  - 何时处理：可选。`npm audit fix`（非破坏性）可尝试。
  - 状态：**暂不处理**。

- **exif-js 库较老**：阶段 9 已换 exifr。
  - 原因：`exif-js@2.3.0` 的 `getStringFromDB` 漏 `var n`，ES module 严格模式报 `n is not defined`；库 2018 停更。
  - 处理：✅ **已换 exifr@7.1.3**（现代 Promise API），gps.js/PropertiesPanel 兼容 DMS 数组(exif-js)和十进制(exifr)。

---

## 迁移完整性审查（2026-07-27，重构后）

> 对比原版 `D:\repos\相册浏览器`（原生 JS）逐模块 fan-out 审查。完整报告见 [docs/superpowers/specs/2026-07-27-迁移完整性审查.md](docs/superpowers/specs/2026-07-27-迁移完整性审查.md)。

### ✅ 已修复（7 个确定性 bug）
- **右键菜单位置不跟随**：定位写在 `watch(visible)`，连续右键 visible 终态恒 true 不触发；改监听 `[visible,x,y]` + 模板 `:style` 响应式绑定（`ContextMenu.vue`），`show()` 去多余 `hide()`（`contextMenu.js`）。
- **触摸单指拖图触发页面滚动**：`onTouchMove` 单指分支补 `preventDefault`（`useModal.js`）。
- **触摸双指缩放下限用错常量**：`MIN_SCALE`(0.1) → `minScale.value`，与滚轮一致（`useModal.js`）。
- **EXIF ISO / 曝光补偿不显示**：exifr 输出 `ISO`/`ExposureCompensation`，代码沿用 exif-js 旧名 `ISOSpeedRatings`/`ExposureBias`；改 key（`PropertiesPanel.vue`）。
- **刷新当前目录丢失**：Gallery 加刷新按钮，`allMediaFolder` 走 `reloadProject`、否则 `refreshFolder`（`Gallery.vue` + `gallery.css`）。
- **拖图片到外部下载失效**：`onDragstart` 补回 `text/uri-list`/`text/plain`/`DownloadURL`，`effectAllowed` 改 `'all'`（`PhotoCard.vue`）。

### 🟡 中严重度（部分已修，2026-07-27）
- ✅ **已修**：搜索 debounce · 空状态提示 · refreshFolder 失效切走 currentFolder · ALL_MEDIA 删文件夹重聚合（递归清后代 foldersData 缓存）· formatFileSize 去尾零。
- ⬜ **未修（低优先级）**：滚动感应区可视化指示条 · 清缓存后主动重绘缩略图 · Ctrl+C 复制 toast 反馈（改动分散，价值低）。
- ✅ **已补修（2026-07-27 验收后）**：属性面板内点击重命名（文件名点击触发，调 `history.renameFile`）· modal 触摸穿透（`onTouchStart` 单指补 `preventDefault` 阻止合成 click 穿透 + 禁 tap-highlight）· 搜索框纳入感应滚动排除区。

### 🪦 已清理（死代码，2026-07-27）
- ✅ `TreeNode` 删 `syncChildren`/`addChild`/`removeChild`/`children`/`active`/`setActive`/`setInactive`（响应式下无用，外部仅用 `expanded`/`isEmpty`/`toggleExpanded`/`refreshState`/`destroy`）。
- ✅ `modal.css` 删 `.dragging-mode` 死规则（Ctrl 拖拽穿透未迁移，无 JS 添加该 class）。

### 🔧 关键约定现代化（规划）
约定1 去 `.js` 扩展名（机械批量）· 约定2 静态注入→构造注入 · 约定4 全局 CSS→scoped（长期）· 约定5 解冻照搬+清死代码。详见审查报告。

---

_更新规则：每阶段验收后，把新发现的遗留事项追加到对应阶段小节；已处理的标记 ✅ 并注明处理方式。_
