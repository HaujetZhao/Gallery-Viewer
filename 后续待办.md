# 后续待办

> 2026-07-28 清理:已处理事项(阶段 0-10 修复 + 架构重构 round1-3 + flush 修复 + 迁移审查 7 bug + 约定现代化)已移除。
> 剩余仅为低优先级 polish + 已决定不做的取舍。历史迁移审查报告见 [docs/superpowers/specs/2026-07-27-迁移完整性审查.md](docs/superpowers/specs/2026-07-27-迁移完整性审查.md)。

## 待办（低优先级,价值低）

- **滚动感应区可视化指示条**:边缘拖拽触发滚动时无视觉提示(功能正常,纯体验)。
- **清空所有缓存后当前视图不自动重绘**:`onClearAll`([SettingsPanel.vue](src/components/SettingsPanel.vue))只清 IDB + toast,不 `triggerRedraw`;当前 Gallery 仍显示旧缩略图直到切走再回。有独立「重绘当前」按钮兜底,价值低。
- **Ctrl+C 复制无 toast 反馈**:`useModal.copyCurrent` 复制后无提示(改动分散,价值低)。

## 用户手动补充（T17 客观修复已完成，待产品负责人主观验收）

> 以下 4 条已由 [T17](docs/superpowers/plans/2026-07-29-技术债治理-T17-modal体验修复.md) 客观修复（lint / test 92 用例 / 双 build 全过），手感请产品负责人上手验收。

- **T17**　当前 model 里面，对于分辨率比较大的照片，打开图片后会按原比例放到大适应视窗，只能放大，不能继续再往小缩。 → **已修**：useModal `initializeMediaDisplay` 不再锁 `minScale=initial`，允许缩到 MIN_SCALE(0.1)。
- [T17] 鼠标在侧栏中仍然会触发感知滚动，这是不应该的。 → **已修**：useScrollZone `isInExcluded` 加 `el.contains(target)` DOM 层级检查（不受 fixed/rect 塌缩影响）。
- [T17] 除在感知滚动的状态中，点击图片打开 modal 的时候，感知滚动会一直保持。 → **已修**：useScrollZone `watch modal isOpen`，open 立即 `stop()` 在途 rAF 滚动。
- [T17] 打开较大的图片到 modal ，或者在 modal 里面切换图片的时候，会发现是有一张大图闪了一下，然后再缩放到适合屏幕。 → **已修**：useModal `fitted` 标志 + MediaModal img `opacity`，fit 完成前隐藏避免原图闪现。

### round4 第一性原理审查后的剩余项(性能目标已达标,这些是 polish)

- **metadata image 读尺寸走 getImageInfoFromHeader**(P1):当前 `new Image()` 重新解码读 dimensions,[utils/file.js](src/utils/file.js) 已有零解码的 `getImageInfoFromHeader`,复用省一次解码。
- **thumbnail-strategies 与 metadata strategy 表去重**(P1):两套平行 strategy 表 + cover-crop 数学重复,抽共用 `utils/image-math.js`。
- **持久化调度 per-root timer**(P2):`Map<rootId, timer>` 简化切根竞态防线(当前模块单例 + cancel/flush 两手)。
- **ALL_MEDIA 增量化**:当前每次切 ALL_MEDIA 全量重拼 O(整树);频繁切 + 万图才痛。
- **md5 计算搬 Web Worker**:防并发算 md5 主线程抖动(可选,单次成本不大)。

## 已决定不做（非缺陷,记录以防再翻）

- **单 HTML build dist 含多余图标**:`public/` 图标被拷进 dist,单 HTML 非「纯单文件」。无害(index.html 自包含);PWA build 又需要这些图标,两套 build 冲突,得不偿失。
- **npm 传递依赖漏洞**(2 moderate / 8 high):纯本地不联网,实际风险低;`npm audit fix` 可试但非必要。
- **md5 缓存键(前 2MB)不改**:内容寻址——跨文件夹/复制副本的同图共享一份缩略图缓存(size+mtime 做不到,mtime 随复制变);md5 随快照持久化 → 秒切零重算;按需视窗计算(非万张预扫)。chunkSize 锁定保旧 IDB key 兼容。详见 [round4](docs/superpowers/specs/2026-07-29-架构重构-round4-第一性原理审查与model函数化.md)。
- **debounce 1s 窗口关浏览器丢改动(残余)**:`schedulePersist` 1s debounce 内硬关标签页,在途写可能丢;P0-3 已加 `visibilitychange:hidden` flush 兜底(hidden 在 pagehide 前触发,多数浏览器允许 IDB 写完)。真正硬关 IDB async 仍不可靠,接受残余风险。根切换窗口已由 `flushPendingPersist` 关闭。

---

_更新规则:新遗留追加到此;已处理直接删除(历史在对应 round 实现记录)。_
