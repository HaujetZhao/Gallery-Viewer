# 架构重构进度记录

记录「资源层分离 + 纯 model + 纯列表 scan」重构的逐阶段落地。每阶段独立可验收、可回滚。

- **设计基线**:[2026-07-28-架构重构-资源层分离与纯model-design.md](./2026-07-28-架构重构-资源层分离与纯model-design.md)(§1–§6 完整背景 + 第一性原理 + 四阶段路径)
- **执行风格**:中文;Windows + PowerShell;独立活儿优先派 subagent;UI 主观体验用户自验;核心算法改动配测试;Ponytail(最小 diff / YAGNI / 删优于增)。

---

## Phase 1:文件资源池 ✅

- **日期**:2026-07-28
- **提交**:`b8aad05`(实现) + `87e620f`(code review 小修)
- **状态**:代码完成 + 审查通过,**待用户浏览器验收**

### 目标
把 SmartFile 的 `blobUrl`/`file`/`_size`/`_lastModified` 四个字段从实例抽到独立资源池 `fileResource`,集中 URL 生命周期(杀 blobUrl 泄漏类 bug),并为 Phase 2 纯列表 scan 解锁(scan 构造 SmartFile 不再被迫 getFile)。**本轮 scan 仍急切 getFile(行为等价),只是结果灌进池而非字段。**

### 核心设计:owner-based 门面
- 新建 [fileResource.js](../../../src/services/fileResource.js):`Map<SmartFile, entry>` 引用计数池,API `acquire/release/destroy/peek`。
- SmartFile 当池门面:`blobUrl`/`size`/`lastModified` 从字段改 getter(读池或 `_meta` 快照槽),`ensureBlobUrl`=acquire、`dispose`=destroy;新增 `_meta={size,lastModified}` 快照槽(fromSnapshot 填,repeat-open 零 IO 依赖);保留 `md5`。
- **收益**:消费方读 `file.blobUrl`/`file.size` 语法不变(字段读==getter 读),8 个消费方零改动——diff 比文档字面"改 6 消费方"小得多。

### 改动文件
| 文件 | 改动 |
|---|---|
| [src/services/fileResource.js](../../../src/services/fileResource.js) | 新建,owner-based 引用计数池 |
| [src/models/SmartFile.js](../../../src/models/SmartFile.js) | 删 4 字段→getter 门面;`_meta` 槽;rename/dispose/ensureBlobUrl 走池 |
| [src/models/SmartFolder.js](../../../src/models/SmartFolder.js) | scan 新建/校验路径改调池(preloaded 复用已 fetch 的 File,零重复 IO) |
| [src/services/operations.js](../../../src/services/operations.js) | `.trash` undo 重建资源改调池(**文档漏列的第 7 处消费方**) |
| [src/services/metadata.js](../../../src/services/metadata.js) | 清 `file.handle ? ... : file.file` 死分支 |
| [src/services/file-resource.test.js](../../../src/services/file-resource.test.js) | 新建,10 例(引用计数/归零 revoke/preloaded 复用/peek) |
| [src/models/smart-file.test.js](../../../src/models/smart-file.test.js) / [smart-folder.test.js](../../../src/models/smart-folder.test.js) | 适配新构造签名 |

### 审查结论(subagent 两阶段 + controller 判断)
- **Spec 合规 ✅**:独立 grep 确认 `.blobUrl`/`_size`/`_lastModified` 全项目无残留直读;7 处碰字段的全改;scan 校验路径读顺序正确(destroy 前读旧 size/mtime);行为等价。
- **Code quality ✅ 无 Critical**。两个 controller 判断:
  - **I1(并发 acquire 同 file 泄漏 URL)**:trace 过 Phase 1-2 无并发同 file 调用模式 → 加前置约束**注释**而非 in-flight 去重代码(文档 §8 YAGNI,随 handle-identity 共享升级再加)。
  - **M3(文件名改 kebab)**:**拒绝**——reviewer 漏看 `handleStore.js`/`scanCache.js` 也是 camelCase,`fileResource.js` 与之一致,项目本就混用。
- 其余:I2/I3 release 语义注释、M1 删测试空壳、M2 rename 重建失败 warn——均已修(`87e620f`)。

### 测试 / lint
9 文件 57 例全绿,lint 干净。
> ⚠️ 沙箱 jsdom 启动 ~21s 异常慢,偶发触发 vitest `failed to find the runner` 竞态(flaky 全炸,重跑即绿),**非 Phase 1 引入、非代码问题**,基线同样。

### 用户验收点
缩略图(图/视频/音频/GIF/SVG)、弹窗、音频播放、拖拽到外部、Ctrl+C 复制、重命名、删除+撤销(`.trash`)、多文件夹切换、属性面板 size/日期。

---

## Phase 2:纯列表 scan + 后台并发补全 ✅

- **日期**:2026-07-28
- **状态**:代码完成 + 审查通过(Spec ✅ / Quality ✅ 无 Critical) + I1 已修,**待用户浏览器验收**

### 目标
把 `SmartFolder.scan` 拆成「`scan` = 纯列名差集(零 getFile,瞬间出列表)」+「`enrich` = 后台并发 getFile 补 size/mtime」。大文件夹首开"瞬间出列表"(名字 + 排序),enrich 在后台并发补全后,Vue 响应式触发 sort 重排。**sort-by-size/date 在 enrich 前的 UX = 瞬时出 + 补全后重排**(缺省项兜底排末尾,无 loading 态,列表可能轻微跳序);name 排序(默认)零依赖不受影响。

### 核心设计
- **scan() = 纯列名差集(零 getFile)**:保留为实例方法(Phase 3 才纯函数化)。
  - values() 列名 + 差集(名字集合比对)+ 信任短路(名字集合一致 → 零 IO 沿用缓存)。
  - 新建文件 `new SmartFile({ handle, parent })`——**不 acquire、不 getFile、_meta=null、md5=null**。
  - **删除非 trust 校验路径**(原 ⑤ 里 size/mtime 校验 + 就地刷新):Phase 2 简化为纯名字集合。文档 §2.3:名字集合是最优信号,原地同名替换罕见,reload 兜底抓增删改名即可。
  - 零 getFile → 秒出。
- **enrich({ token }) 实例方法(新)**:补 `this.files` 里"待补"的文件。
  - targets = `this.files.filter(f => peek(f) == null && f._meta == null)`(listFolder 新建的、池空且无快照的)。信任短路后既有文件(_meta 有)→ targets 空 → 信任路零 getFile。
  - `await runConcurrent(targets, async f => { const file = await f.handle.getFile(); await acquire(f, f, file); f._meta = { size, lastModified }; }, { concurrency: SCAN_CONCURRENCY, token })`。
  - **acquire 灌池(url/file)+ 写 _meta 触发 Vue 响应式**(关键)。
- **size/lastModified getter `_meta` 优先**(响应式陷阱,类比 be2ffe3):`_meta` 是 SmartFile 实例字段(在 store 的 reactive files 数组里,属性变更触发 Vue 响应式);peek 读普通 Map(不响应式)。enrich 写 _meta → Gallery sort computed 重排。若仍优先 peek,enrich 后 size 变了 Vue 不知道,sort 不重排。`?? peek` 兜底(_meta 漏写时)。
- **所有 getFile 处补 _meta**(响应式一致性):除 listFolder(故意不写,_meta=null 是"size 暂缺"来源),其他 getFile 处写 _meta——operations.js `FileDeleteOperation.undo`、SmartFile.rename(move 后读 entry.file 写 _meta)。
- **Gallery sort NaN 兜底**:enrich 前 size/lastModified undefined → NaN 排序乱。兜底 `(a.size ?? Infinity) - (b.size ?? Infinity) * dir`(undefined 排末尾)。enrich 写 _meta 后响应式重排。
- **消费方 ensureBlobUrl 连锁**:listFolder 零 getFile → 新文件 peek 空 → blobUrl null。读 blobUrl 的消费方在 enrich 前调会拿到 null,加 ensureBlobUrl(懒 acquire,复用 enrich 的池,不重复 IO):
  - thumbnail.js `generateThumbnail` 开头 `await file.ensureBlobUrl()`;md5 计算复用 `peek(file)?.file`(省一次 getFile)。
  - MediaModal.vue watch currentFile:**所有类型**先 ensureBlobUrl(image/video 直接读 blobUrl,audio/svg 靠 ensureBlobUrl 后 peek 有 url)。
  - metadata.js 4 strategy getMetadata 开头各加 `await file.ensureBlobUrl()`(属性面板晚调,边界保险)。
  - useModal.js `copyCurrent`(Ctrl+C 复制)复用 `peek(file)?.file`(Modal 已 ensureBlobUrl)。
  - **PhotoCard onDragstart 不动**(同步不能 await,注释已说"blobUrl 可能 null,外部 MIME 跳过",enrich 后正常)。

### 改动文件
| 文件 | 改动 |
|---|---|
| [src/models/SmartFolder.js](../../../src/models/SmartFolder.js) | scan 拆纯列名(删 ④/⑤ getFile 校验/并发 getFile);新增 `enrich({token})` 后台并发 getFile 写 _meta;import 改 `{ acquire, peek }` |
| [src/models/SmartFile.js](../../../src/models/SmartFile.js) | size/lastModified getter 改 `_meta` 优先(`?? peek`);rename move 后读 entry.file 写 _meta |
| [src/services/operations.js](../../../src/services/operations.js) | `FileDeleteOperation.undo` acquire 后加 `this.fileData._meta = { size, lastModified }` |
| [src/components/Gallery.vue](../../../src/components/Gallery.vue) | sort NaN 兜底:`(a.size ?? Infinity) - (b.size ?? Infinity)` |
| [src/services/thumbnail.js](../../../src/services/thumbnail.js) | `generateThumbnail` 开头 `await file.ensureBlobUrl()`;md5 计算复用 `peek(file)?.file`;import peek |
| [src/components/MediaModal.vue](../../../src/components/MediaModal.vue) | watch currentFile 改:所有类型先 ensureBlobUrl,再按 mediaKind 设 imgSrc |
| [src/services/metadata.js](../../../src/services/metadata.js) | 4 strategy(image/video/audio/svg)getMetadata 开头各加 `await file.ensureBlobUrl()` |
| [src/composables/useModal.js](../../../src/composables/useModal.js) | `copyCurrent` 复用 `peek(file)?.file`;import peek |
| [src/services/filesystem.js](../../../src/services/filesystem.js) | `startBackgroundScan` 进入节点先 enrich 当前层,再遍历子目录;`refreshFolder` scan + enrich;`reloadProject` 简化注释(去非 trust 校验语义) |
| [src/models/smart-folder.test.js](../../../src/models/smart-folder.test.js) | 重写 scan 用例(零 getFile:files 有但 size undefined、getFile 未调);新增 enrich 补全用例;信任短路加 enrich targets 空;信任差异(新增)改:scan 零 getFile + enrich 只补新增;**删除**非 trust size 校验用例(行为简化掉) |
| [src/services/filesystem.test.js](../../../src/services/filesystem.test.js) | fakeFolder mock 加 enrich(空实现) |

### 审查结论
- **Spec 合规 ✅**:scan 函数体 awk+grep 双验证**零 getFile**;enrich 响应式 _meta 正确;size getter `_meta` 优先;消费方 ensureBlobUrl 5 处全覆盖(thumbnail/MediaModal 全类型/metadata 4 strategy/useModal copyCurrent);Gallery NaN 兜底 + dir 优先级正确;reload 行为变化符合 §2.3。57/57 连跑 2 次绿,lint 干净。
- **Code quality ✅ 无 Critical**。controller 判断与处置:
  - **I1 已修**(enrich 后台 + 缩略图 ensureBlobUrl 并发 acquire 同 file → blob URL 泄漏):Phase 1 推迟的 in-flight 去重债,Phase 2 真实复活(首开高频触发)。修复 = acquire 加 in-flight promise 去重(并发共享一次 getFile + 一个 url) + ensureBlobUrl peek 短路 + 并发不泄漏测试守护。
  - **I2 核实排除**(reload 同名替换检测倒退):reviewer 误判——reloadProject → `initProject` 会 `foldersData.clear()` 后**重建所有 SmartFile**(新对象 peek 空),enrich 全量重新 getFile,reload **能**抓同名替换。非倒退。
  - **I3 留 Phase 3**(enrich 逐个写 _meta 触发 N 次重排 + Gallery 无虚拟化,大文件夹首开可能卡):用户选了"瞬时出 + 补全后重排"属可接受 UX;若卡顿,Phase 3 做 Gallery 虚拟化 / requestAnimationFrame 批处理。
  - M1(SmartFile 注释 scan→enrich)、M3(filesystem.test enrich 调用断言)已修;M2(metadata EXIF 仍绕池)/M4(注释措辞)/M5(recovery 传原始对象,响应式链路边角)列入 Phase 3 清理。

### 测试 / lint
9 文件 57 例全绿(连跑 2 次确认,防 jsdom 启动慢 flaky),lint 干净。
> ⚠️ 沙箱 jsdom 启动 ~21s 异常慢,偶发触发 vitest `failed to find the runner` 竞态(flaky 全炸,重跑即绿),**非 Phase 2 引入**,基线同样。

### 行为变化(用户验收用)
1. **scan 不再同步 getFile**:大文件夹首开"瞬间出列表"(name 排序零依赖,体验最佳);sort-by-size/date 在 enrich 前缺省项排末尾,enrich 完成后(通常秒级)响应式自动重排,可能轻微跳序,无 loading 态。
2. **reload 不再 size/mtime 校验**:Phase 2 简化为纯名字集合 + enrich 新增项。原地同名同尺寸替换(罕见)检测不到,文档 §2.3 接受(reload 兜底抓增删改名)。
3. **新文件 blobUrl 懒**:enrich 完成 blobUrl 前 null,消费方(thumbnail/MediaModal/metadata)ensureBlobUrl 懒建(复用 enrich 池,不重复 IO);PhotoCard onDragstart 同步不能 await,enrich 后正常拖拽。

### 用户验收点
- 大文件夹(>200 文件)首开瞬出列表(name 排序),size/date 排序瞬出后 enrich 重排(轻微跳序)。
- 缩略图(图/视频/音频/GIF/SVG)、弹窗(Modal 全类型)、音频播放、拖拽到外部、Ctrl+C 复制、重命名、删除+撤销(`.trash`)、多文件夹切换、属性面板 size/日期。
- 边界:enrich 完成前打开 Modal / 属性面板 / 缩略图 → ensureBlobUrl 懒建兜底,不出现图裂/null size。

---

## Phase 3:纯 model + store/service 整合 ✅

- **日期**:2026-07-28
- **提交**:`20b540e`(Step 1 实现) + `6173f5f`(Step 1 review 注释) + `cab2d62`(Step 2)
- **状态**:代码完成 + 审查通过(Step 1 spec✅/quality✅;Step 2 grep+test 自核),**待用户浏览器验收**

### 目标
消灭响应式陷阱类(model 自改 `this` + appState 静态注入)。model 纯函数(算,不改 this/不碰 foldersData/不 dispose);store/service 拿结果对**代理** folder 整合(写回树 + 注册 foldersData + dispose);删 appState。使 be2ffe3 那类 bug 从结构上不可能。

### 核心设计
- **scanFolder 纯函数**(Step 1):模块级,替代实例 scan。复用算法(values 列名 + 差集 + 信任短路),只去副作用——不改 folder 入参、不碰 foldersData、不 dispose;返回 `{files, subFolders, newFiles, newSubFolders, removedFiles, removedFolders}`。`SmartFolder.create` 委托(不写回)。
- **integrateScanResult helper**(Step 1):集中副作用——写回代理 folder.files/subFolders/scanned + refreshState + 注册 newSubFolders + 删 removedFolders + dispose removedFiles。
- **调用方写回代理**(Step 1,根治 be2ffe3):getFolderData 未命中 → **先 foldersData.set(代理化)→ get 取代理 → scanFolder(代理) → integrateScanResult(代理)**;startBackgroundScan 用 parentFolder.subFolders 元素(代理);refreshFolder scanFolder+integrateScanResult;recovery newSubFolders 注册取代理后 startBackgroundScan(修 Phase 2 M5)。
- **fromSnapshot 纯函数 + registerFolderTree**(Step 2):fromSnapshot 不注册 foldersData(返回 folder 树);switchToRoot 递归注册。
- **删 appState**(Step 2):SmartFolder.appState 静态字段 + fs.js 注入 + 测试 mock 全删(grep 0 功能残留)。

### 改动文件
| 文件 | 改动 |
|---|---|
| [src/models/SmartFolder.js](../../../src/models/SmartFolder.js) | scanFolder 纯函数 + create 委托 + fromSnapshot 纯函数 + 删 scan 方法/appState 字段 |
| [src/services/filesystem.js](../../../src/services/filesystem.js) | integrateScanResult + registerFolderTree helper;getFolderData/startBackgroundScan/refreshFolder/switchToRoot 整合写回代理 |
| [src/services/recovery.js](../../../src/services/recovery.js) | scanFolder + integrateScanResult;newSubFolders 注册取代理后 startBackgroundScan(修 M5) |
| [src/stores/fs.js](../../../src/stores/fs.js) | 删 appState 注入;import 改只 createAllMediaFolder |
| [src/models/smart-folder.test.js](../../../src/models/smart-folder.test.js) | scanFolder 纯函数(不改入参) + integrateScanResult + fromSnapshot 不注册 + 删 appState mock |
| [src/services/filesystem.test.js](../../../src/services/filesystem.test.js) | fakeFolder 适配(scanFolder 读 handle.values + Pinia beforeEach) |

### 审查结论
- **Step 1 Spec ✅**:scanFolder 函数体副作用 grep 零命中(真·纯函数);getFolderData 写回**代理**(先 set 取代理,be2ffe3 根治);recovery M5 真修;appState 保留(Step 2 边界);行为等价。
- **Step 1 Quality ✅ 无 Critical**:reviewer 实测 Vue3 响应式(ref(Map) set 原始→get 代理,写代理触发 effect)确认根治机制。I1(注册原始 vs 代理二元性)、I2(信任短路零拷贝)、M3(listFolder→scanFolder)注释已修;M2(集成测试)skip(单元覆盖)。
- **Step 2 自核**:grep appState 0 功能残留(5 处全注释);fs.js 注入删 + import 清理;fromSnapshot 纯函数 + registerFolderTree 注册。

### 测试 / lint
9 文件 59 例全绿(连跑确认),lint 干净。

### 架构收益
- model 层纯逻辑(不反向依赖 store);scanFolder/fromSnapshot 纯函数(可单测,不改入参)。
- 副作用集中 service(integrateScanResult/registerFolderTree),响应式写回代理(根治 be2ffe3 类)。
- 删 appState 静态注入(model→store 反向依赖消除)。

### 用户验收点
- **be2ffe3 场景免疫**:多文件夹切换、打开新文件夹、子目录后台扫描 —— UI 全程同步(子目录不停在半透明 isEmpty)。
- recovery(移走子文件夹再点)—— 子目录 enrich 响应式更新(Phase 2 M5 修复)。
- 回归 Phase 1/2:缩略图/弹窗/音频/拖拽/复制/重命名/删除撤销/属性面板。

---

## Phase 4:清理(低风险收尾)✅

- **日期**:2026-07-28
- **提交**:`44f99c7`
- **状态**:代码完成 + 自核通过(grep 0 残留 + 59/59 绿),**待用户浏览器冒烟**

### 目标
删 Vue3 化后的纯包袱(TreeNode DOM 部分早删,只留 expanded/isEmpty/count,count 没人用)+ 删 scanned 字段(Phase 2 移除唯一消费者,只剩序列化)+ M2(metadata EXIF 绕池)+ M4(注释措辞)。

### 改动
- **删 [TreeNode.js](../../../src/models/TreeNode.js) 整文件**(class + treeNodeRegistry)。
- [SmartFolder.js](../../../src/models/SmartFolder.js):`expanded`→folder.expanded 字段;`isEmpty`→folder.isEmpty **getter**(实时算,免 refreshState);`toggleExpanded`→folder 方法;删 scanned 字段 + toSnapshot/fromSnapshot scanned;删 import TreeNode + constructor treeNode。
- [filesystem.js](../../../src/services/filesystem.js):integrateScanResult/refreshFolder 删 treeNode.refreshState/destroy + scanned;注释清。
- [SidebarTreeItem.vue](../../../src/components/SidebarTreeItem.vue):`folder.treeNode.{toggleExpanded,isEmpty,expanded}` → `folder.{toggleExpanded,isEmpty,expanded}`。
- [metadata.js](../../../src/services/metadata.js):image EXIF `handle.getFile()` → `peek(file)?.file ?? handle.getFile()`(复用池,M2)。
- 测试:smart-folder.test.js/filesystem.test.js 删 scanned/treeNode mock + 断言。

### 自核
grep `treeNode|TreeNode|scanned` src/ **0 功能残留**(仅 SmartFolder.js 2 条追溯注释 + docs 历史)。isEmpty getter 响应式(依赖 files/subFolders.length)。59/59 连跑绿,lint 干净。

### 收益
- 少一个类(TreeNode)+ 一个全局 registry,字段并入 folder(单一来源)。
- isEmpty getter 免手动 refreshState(原漏调会 stale,现永远最新)。
- 删 scanned(死字段);metadata EXIF 复用池(省一次 IO)。

### 用户验收点(冒烟)
- 侧栏树展开/折叠/空状态图标(folder-open/folder/empty-folder)正常。
- snapshot 往返(多文件夹切换)expanded 保持。
- 回归 Phase 1-3。

---

## 重构整体总结(Phase 1-4)

四阶段全部完成,设计文档 §6 路径落地:

| Phase | 目标 | 提交 |
|---|---|---|
| 1 资源池 | blobUrl/file/size/mtime 抽到 fileResource 池,SmartFile 门面 | b8aad05/87e620f |
| 2 纯列表 scan | scan 拆 listFolder(零 getFile 瞬扫)+ enrich(后台补 size/mtime) | d0e43d3/ffb2586 |
| 3 纯 model | scanFolder/fromSnapshot 纯函数 + 删 appState + 写回代理(根治 be2ffe3) | 20b540e/6173f5f/cab2d62 |
| 4 清理 | 删 TreeNode/scanned + metadata peek | 44f99c7 |

**核心成果**:SmartFile 三重耦合(身份/资源/派生)拆开;model 纯函数(不改 this、不反向依赖 store);blobUrl 生命周期集中池;scan 瞬扫;响应式陷阱(be2ffe3 类)从结构上消除。每阶段独立可验收/可回滚,测试全程绿(基线 47 → 现 59)。
