# 阶段 3：缩略图系统 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans. Steps use checkbox (`- [ ]`) syntax.

**Goal:** 迁移缩略图系统：IndexedDB 缓存（db.js）+ 5 种媒体策略（thumbnail-strategies.js）+ 缩略图生成 service。里程碑：dev 扫描文件夹后，点按钮对第一张图片生成缩略图，画到 canvas 显示，且二次生成命中 IndexedDB 缓存。

**Architecture:**
- `db.js` → `src/services/db.js`：**纯逻辑**（initDB/get/set/delete/cleanOldCache 清理），**UI 副作用剥离**（原 `clearAllCache`/`updateStorageUsage`/`cleanOldCache` 里的 toast + `#storageInfoValue` DOM + `redrawAllThumbnails` 移到调用方，阶段 7 settings panel）。修两个陷阱：`saveThumbnailToDB` 返回 Promise（源码 fire-and-forget）；新增 `touchThumbnailInDB` 命中缓存时刷新 `lastAccessed`（源码写入时没写该字段，导致 cleanOldCache 误清）。
- `thumbnail-strategies.js` → `src/services/thumbnail-strategies.js`：**零改动照搬**（仅加 `export` + `import FileTypes`）。5 策略（image/gif/svg/video/audio）+ `extractAudioCover`（ID3 APIC）+ `getThumbnailStrategy`。
- 新建 `src/services/thumbnail.js`：从 `thumbnails.js` 抽出**生成主体** `generateThumbnail(file, canvas, targetSize)`（查缓存→命中画/未命中生成存），不含 IntersectionObserver/队列（那是 gallery 卡片需求，挪到阶段 5 composable）。

**范围调整（对 spec）：** spec 阶段 3 含 `useThumbnails` composable（observer + 队列）。但 observer/队列是 gallery 卡片的需求，阶段 3 没有卡片 DOM。**挪到阶段 5（gallery）**。阶段 3 聚焦：db + strategies + 生成 service。

**Tech Stack:** IndexedDB、canvas、File System Access API（file.handle.getFile）。

**关于测试：** IndexedDB/canvas/视频抓帧/ID3 解析都依赖浏览器 API，node 跑不了。阶段 3 用「dev 生成真实缩略图 + 二次命中缓存」端到端验证。纯逻辑（ID3 synchsafe int 解析、cover 缩放算式）在实际生成时自然验证。

**关于照搬：** thumbnail-strategies.js 已读全文（393 行），照搬；db.js 改造后贴完整；thumbnail.js 新设计贴完整。

---

## File Structure

| 文件 | 职责 | 来源 |
|---|---|---|
| `src/services/db.js` | IndexedDB 封装（initDB/get/save/get/touch/clearAllCache/cleanOldCache/delete），纯逻辑 | db.js（剥 UI + 修陷阱） |
| `src/services/thumbnail-strategies.js` | 5 策略 + extractAudioCover + getThumbnailStrategy | thumbnail-strategies.js（零改动照搬） |
| `src/services/thumbnail.js` | `generateThumbnail(file, canvas, targetSize)` 生成主体 | thumbnails.js 的 generateAndShowThumbnail 抽取（去 observer/DOM 耦合） |

---

## Task 1: services/db.js（纯逻辑 + 修陷阱）

**Files:**
- Create: `src/services/db.js`

- [ ] **Step 1: 写 db.js**

Create `src/services/db.js`（IndexedDB 纯逻辑，UI 副作用剥离，修两个陷阱）:
```js
// IndexedDB 缩略图缓存。搬自源码 js/db.js,纯逻辑(UI 副作用剥离到调用方)。
// 缓存键 id = `${md5}_${width}`。修两个陷阱:save 返回 Promise;新增 touch 刷新 lastAccessed。
import { CONFIG } from '../config/index.js';

const DB_NAME = CONFIG.DATABASE.NAME;
const DB_VERSION = CONFIG.DATABASE.VERSION;
const STORE_NAME = CONFIG.DATABASE.STORES.THUMBNAILS;
let db = null;

export function initDB() {
  return new Promise((resolve, reject) => {
    const request = indexedDB.open(DB_NAME, DB_VERSION);
    request.onupgradeneeded = (e) => {
      const d = e.target.result;
      if (!d.objectStoreNames.contains(STORE_NAME)) {
        d.createObjectStore(STORE_NAME, { keyPath: 'id' });
      }
    };
    request.onsuccess = (e) => {
      db = e.target.result;
      resolve(db);
    };
    request.onerror = reject;
  });
}

export function getDB() {
  return db;
}

// 源码原本 fire-and-forget,改为返回 Promise(等 tx.oncomplete),便于调用方确认落盘。
export function saveThumbnailToDB(data) {
  return new Promise((resolve) => {
    if (!db) {
      resolve();
      return;
    }
    try {
      const tx = db.transaction([STORE_NAME], 'readwrite');
      tx.objectStore(STORE_NAME).put(data);
      tx.oncomplete = () => resolve();
      tx.onerror = () => resolve();
    } catch (e) {
      console.error('DB Save Error', e);
      resolve();
    }
  });
}

export function getThumbnailFromDB(md5, width) {
  return new Promise((resolve) => {
    if (!db) {
      resolve(null);
      return;
    }
    const req = db
      .transaction([STORE_NAME], 'readonly')
      .objectStore(STORE_NAME)
      .get(`${md5}_${width}`);
    req.onsuccess = (e) => resolve(e.target.result);
    req.onerror = () => resolve(null);
  });
}

// 命中缓存时刷新 lastAccessed(源码写入时只存 timestamp,cleanOldCache 读 lastAccessed||timestamp,
// 导致按创建时间清理。命中时 touch 一下,让最近访问的缓存不被误清)。
export function touchThumbnailInDB(id) {
  return new Promise((resolve) => {
    if (!db) {
      resolve();
      return;
    }
    const tx = db.transaction([STORE_NAME], 'readwrite');
    const store = tx.objectStore(STORE_NAME);
    const req = store.get(id);
    req.onsuccess = () => {
      const record = req.result;
      if (record) {
        record.lastAccessed = Date.now();
        store.put(record);
      }
    };
    tx.oncomplete = () => resolve();
    tx.onerror = () => resolve();
  });
}

// 清空全部。返回 Promise,UI 副作用(toast/redraw)由调用方处理。
export function clearAllCache() {
  return new Promise((resolve) => {
    if (!db) {
      resolve();
      return;
    }
    const tx = db.transaction([STORE_NAME], 'readwrite');
    const req = tx.objectStore(STORE_NAME).clear();
    req.onsuccess = () => resolve();
    req.onerror = () => resolve();
  });
}

// 清理 20 天未访问的。返回 { deletedCount },UI 副作用由调用方。
export function cleanOldCache() {
  return new Promise((resolve) => {
    if (!db) {
      resolve({ deletedCount: 0 });
      return;
    }
    const DAYS_20 = 20 * 24 * 60 * 60 * 1000;
    const threshold = Date.now() - DAYS_20;
    const tx = db.transaction([STORE_NAME], 'readwrite');
    const store = tx.objectStore(STORE_NAME);
    const req = store.openCursor();
    let deletedCount = 0;
    req.onsuccess = (event) => {
      const cursor = event.target.result;
      if (cursor) {
        const lastTime = cursor.value.lastAccessed || cursor.value.timestamp || Date.now();
        if (lastTime < threshold) {
          cursor.delete();
          deletedCount++;
        }
        cursor.continue();
      }
    };
    tx.oncomplete = () => resolve({ deletedCount });
    tx.onerror = () => resolve({ deletedCount });
  });
}

export function deleteThumbnail(id) {
  if (!db) return;
  db.transaction([STORE_NAME], 'readwrite').objectStore(STORE_NAME).delete(id);
}
```

> 注：源码 `updateStorageUsage`（写 `#storageInfoValue` DOM）不迁，阶段 7 settings panel 用 `navigator.storage.estimate()` 自取。

- [ ] **Step 2: Commit**

```bash
cd "d:/repos/相册浏览器-pwa" && git add src/services/db.js && git commit -m "feat(阶段3): IndexedDB 封装(纯逻辑+剥 UI+修 lastAccessed/save Promise 陷阱)"
```

---

## Task 2: services/thumbnail-strategies.js（照搬）

**Files:**
- Create: `src/services/thumbnail-strategies.js`

- [ ] **Step 1: 照搬源码**

读 `D:\repos\相册浏览器\js\thumbnail-strategies.js` 全文，迁移到 `src/services/thumbnail-strategies.js`：

**改造点（仅这些，其余一字不改）：**
1. 文件顶部加 `import { FileTypes } from '../config/file-types.js';`
2. `const ThumbnailStrategies = {...}` → `export const ThumbnailStrategies = {...}`
3. `async function extractAudioCover(fileData) {...}` → `export async function extractAudioCover(fileData) {...}`
4. `function getThumbnailStrategy(fileType) {...}` → `export function getThumbnailStrategy(fileType) {...}`

**5 个策略逐字照搬**（image/gif/svg/video/audio），含：
- `image`：canvas cover 缩放（`Math.max` ratio + centerShift），输出 jpeg 0.85。
- `gif`：`<img>` 挂 blobUrl，不缓存。
- `svg`：`<object type="image/svg+xml">` 加载。
- `video`：临时 `<video>` 抓第 5 秒/中点帧，`drawVideoFrame` cover 缩放，10s 超时走 `drawDefaultThumbnail`（紫蓝渐变 + ▶ U+25B6）。
- `audio`：先 `extractAudioCover` 提 ID3 APIC 封面（cover 缩放），提不到画紫蓝渐变 + 🎵 emoji。
- `extractAudioCover`：ID3v2 头检测（`ID3` = 0x49 0x44 0x33）+ synchsafe int 标签大小 + v2.3/v2.4 帧大小差异 + APIC 帧解析（跳 textEncoding/mimeType/pictureType/desc，取图片数据）。逐字符照搬。
- `getThumbnailStrategy`：线性扫描 `strategy.types.includes(fileType)`，fallback image。

> 注：`drawDefaultThumbnail`/audio 里 `ctx.font = '...px "Font Awesome 6 Free"'` + 画 `▶`/`🎵`——这俩是 Unicode/emoji（U+25B6 / 🎵），不依赖 FA 字体名，浏览器 fallback 即可渲染，照搬保留。

- [ ] **Step 2: 验证 import 链**

Run:
```bash
cd "d:/repos/相册浏览器-pwa" && node --input-type=module -e "
const m = await import('./src/services/thumbnail-strategies.js');
console.log('策略数:', Object.keys(m.ThumbnailStrategies).length);
console.log('image types:', m.ThumbnailStrategies.image.types.length, '| video badge:', JSON.stringify(m.ThumbnailStrategies.video.getCardBadge()));
console.log('getThumbnailStrategy(jpg).name:', m.getThumbnailStrategy('jpg').name);
console.log('getThumbnailStrategy(mp4).name:', m.getThumbnailStrategy('mp4').name);
console.log('getThumbnailStrategy(mp3).name:', m.getThumbnailStrategy('mp3').name);
"
```
Expected: 策略数 5；video badge 含 `fa-play-circle/VIDEO/badge-video`；jpg→image、mp4→video、mp3→audio。

- [ ] **Step 3: Commit**

```bash
cd "d:/repos/相册浏览器-pwa" && git add src/services/thumbnail-strategies.js && git commit -m "feat(阶段3): 缩略图策略(5 策略 + ID3 封面提取,零改动照搬)"
```

---

## Task 3: services/thumbnail.js（生成主体）

**Files:**
- Create: `src/services/thumbnail.js`

- [ ] **Step 1: 写 thumbnail.js**

Create `src/services/thumbnail.js`（从 thumbnails.js 的 generateAndShowThumbnail 抽取生成主体，去 observer/DOM 耦合）:
```js
// 缩略图生成主体。搬自源码 js/thumbnails.js 的 generateAndShowThumbnail,去 observer/队列/DOM 耦合。
// 接收 (file: SmartFile, canvas: HTMLCanvasElement, targetSize) → 查缓存→命中画/未命中生成存→画到 canvas。
// IntersectionObserver + 并发队列留到阶段 5 gallery 的 useThumbnail composable。
import { calculateMD5 } from '../utils/file.js';
import { getThumbnailStrategy } from './thumbnail-strategies.js';
import { getThumbnailFromDB, saveThumbnailToDB, touchThumbnailInDB } from './db.js';

// 把缓存 blob 画到 canvas(缓存恢复,不做缩放,blob 本就是 targetSize 方图)。
function drawBlobToCanvas(canvas, blob) {
  return new Promise((resolve, reject) => {
    const img = new Image();
    const url = URL.createObjectURL(blob);
    img.onload = () => {
      canvas.width = img.width;
      canvas.height = img.height;
      canvas.getContext('2d').drawImage(img, 0, 0);
      URL.revokeObjectURL(url);
      resolve();
    };
    img.onerror = (e) => {
      URL.revokeObjectURL(url);
      reject(e);
    };
    img.src = url;
  });
}

// 生成缩略图并画到 canvas。返回 { cached, strategyName }。
// GIF/SVG 不缓存,直接渲染;image/video/audio 走 IndexedDB 缓存(md5 + targetSize 为键)。
export async function generateThumbnail(file, canvas, targetSize = 400) {
  const strategy = getThumbnailStrategy(file.type);

  // GIF/SVG 快路径:不缓存
  if (strategy.name === 'gif' || strategy.name === 'svg') {
    await strategy.generateThumbnail(canvas, file, targetSize);
    return { cached: false, strategyName: strategy.name };
  }

  // 缓存路径:image/video/audio。md5 懒加载(首次算后存 file.md5)。
  if (!file.md5) {
    const raw = await file.handle.getFile();
    file.md5 = await calculateMD5(raw);
  }
  const cached = await getThumbnailFromDB(file.md5, targetSize);

  if (cached) {
    await drawBlobToCanvas(canvas, cached.blob);
    // 异步刷新 lastAccessed(不阻塞,修源码陷阱)
    touchThumbnailInDB(`${file.md5}_${targetSize}`);
    return { cached: true, strategyName: strategy.name };
  }

  // 未命中:策略生成(已画到 canvas)→ 存 DB
  const blob = await strategy.generateThumbnail(canvas, file, targetSize);
  if (blob) {
    await saveThumbnailToDB({
      id: `${file.md5}_${targetSize}`,
      md5: file.md5,
      size: file.size,
      width: targetSize,
      timestamp: Date.now(),
      blob,
    });
  }
  return { cached: false, strategyName: strategy.name };
}
```

- [ ] **Step 2: 验证 import 链**

Run:
```bash
cd "d:/repos/相册浏览器-pwa" && node --input-type=module -e "
const m = await import('./src/services/thumbnail.js');
console.log('generateThumbnail:', typeof m.generateThumbnail);
"
```
Expected: 打印 `generateThumbnail: function`（确认 thumbnail.js → strategies → db → utils import 链无误）。

- [ ] **Step 3: Commit**

```bash
cd "d:/repos/相册浏览器-pwa" && git add src/services/thumbnail.js && git commit -m "feat(阶段3): 缩略图生成 service(generateThumbnail 查缓存/生成/存 DB)"
```

---

## Task 4: App.vue 临时缩略图验收

**Files:**
- Modify: `src/App.vue`

- [ ] **Step 1: App.vue 加缩略图生成按钮 + canvas**

在 `src/App.vue` 的 `<script setup>` 加 initDB + generateThumbnail 调用，template 加 canvas + 按钮。

`<script setup>` 增加导入与函数：
```js
import { ref, onMounted } from 'vue';
import { initDB } from './services/db.js';
import { generateThumbnail } from './services/thumbnail.js';
import { FileTypes } from './config/file-types.js';

const thumbCanvasRef = ref(null);
const thumbStatus = ref('');

onMounted(async () => {
  themeStore.init();
  try {
    await initDB();
  } catch (e) {
    console.warn('initDB 失败:', e);
  }
});

// 临时:对当前文件夹第一张图片生成缩略图。阶段 5 gallery 接入真实卡片后替换。
async function generateFirstThumbnail() {
  const folder = fsStore.currentFolder;
  if (!folder) {
    thumbStatus.value = '请先扫描文件夹';
    return;
  }
  const file = folder.files.find((f) => FileTypes.image.standard.includes(f.type));
  if (!file) {
    thumbStatus.value = '根目录无标准图片(jpg/png/webp/bmp/jfif)';
    return;
  }
  const canvas = thumbCanvasRef.value;
  const t0 = performance.now();
  try {
    const result = await generateThumbnail(file, canvas, 400);
    const dt = Math.round(performance.now() - t0);
    thumbStatus.value = `${file.name} · ${result.strategyName} · ${result.cached ? '缓存命中' : '新生成'} · ${dt}ms`;
    console.log('缩略图结果:', result, '| md5:', file.md5);
  } catch (e) {
    thumbStatus.value = `生成失败: ${e.message}`;
    console.error(e);
  }
}
```

> 注：原有 `import { onMounted } from 'vue'` 改为 `import { ref, onMounted } from 'vue'`（加 ref）。`themeStore.init()` 保留。

`<template>` 在 `.scan-zone` 之后加：
```html
<div class="thumb-zone">
  <button class="scan-btn" @click="generateFirstThumbnail" :disabled="!fsStore.currentFolder">
    🖼 生成第一张图片缩略图
  </button>
  <canvas ref="thumbCanvasRef" width="400" height="400" class="thumb-canvas"></canvas>
  <span class="scan-result">{{ thumbStatus }}</span>
</div>
```

`<style scoped>` 追加：
```css
.thumb-zone {
  margin-top: 20px;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8px;
}
.thumb-canvas {
  width: 200px;
  height: 200px;
  border: 1px solid var(--color-gray-300, #ced4da);
  border-radius: 8px;
  background: var(--bg-tertiary, #ecf0f1);
}
.scan-btn:disabled {
  opacity: 0.4;
  cursor: not-allowed;
}
```

- [ ] **Step 2: 验证 build 通过**

Run:
```bash
cd "d:/repos/相册浏览器-pwa" && npm run build 2>&1 | tail -8
```
Expected: 构建成功。

- [ ] **Step 3: Commit**

```bash
cd "d:/repos/相册浏览器-pwa" && rm -rf dist && git add src/App.vue && git commit -m "feat(阶段3): App.vue 临时缩略图生成验收(canvas + IndexedDB 缓存)"
```

- [ ] **Step 4: 交付用户 dev 验收**

向用户报告阶段 3 完成情况 + 请 `npm run dev` 验收：
1. 先点「📂 打开文件夹并扫描」选一个含图片的目录。
2. 再点「🖼 生成第一张图片缩略图」→ canvas 显示第一张图的中心裁剪方图（400×400，cover 模式）。
3. 按钮下方状态：`<文件名> · image · 新生成 · <N>ms`，控制台打印 md5。
4. **再点一次**「生成缩略图」→ 状态变 `缓存命中`，且耗时明显更短（验证 IndexedDB 缓存生效 + lastAccessed 刷新）。
5. F12 → Application → IndexedDB → GalleryThumbnailDB → thumbnails，能看到一条 `id=<md5>_400` 的记录。
6. 控制台无报错。
7. （可选）根目录放个 mp4，临时把 find 条件改成 video 类型，验证视频抓帧（或留到阶段 5）。

用户确认后 commit 阶段 3 里程碑：
```bash
cd "d:/repos/相册浏览器-pwa" && git commit --allow-empty -m "chore: 阶段 3(缩略图系统)完成,用户 dev 验收通过"
```

---

## Self-Review（已执行）

1. **Spec 覆盖**：spec 阶段 3（db/thumbnail-strategies/缩略图生成）覆盖；`useThumbnails` composable 明确挪到阶段 5（已说明）。✓
2. **占位扫描**：db.js/thumbnail.js 代码完整；thumbnail-strategies.js 标注「照搬源码全文」（已读，在手）。✓
3. **命名/接口一致**：`generateThumbnail(file, canvas, targetSize)`、`getThumbnailStrategy`、`saveThumbnailToDB/getThumbnailFromDB/touchThumbnailInDB`、缓存键 `${md5}_${targetSize}` 跨 task 一致。✓
4. **陷阱修复**：saveThumbnailToDB 返回 Promise、touchThumbnailInDB 刷新 lastAccessed 均已加。✓
5. **CONFIG 字段**：阶段 1 已有 `CONFIG.DATABASE = { NAME, VERSION, STORES.THUMBNAILS }`，db.js 直接用。✓
