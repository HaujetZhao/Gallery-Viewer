# 阶段 1：配置基础 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 迁移源码 `config.js / file-types.js / themes.js / browser-compatibility.js / utils.js` 五个模块到 Vue + Pinia。里程碑：`npm run dev` 能切换 3 套主题（海洋蓝/暗色/森林绿）并持久化。

**Architecture:** 纯逻辑层（`CONFIG` / `FileTypes` / utils）原样 `export` 去 global 化；`UserSettings` 与 `ThemeManager` 进 Pinia setup store（UI 设置面板需要响应式）；主题切换保留源码 `document.documentElement.style.setProperty` 注入 CSS 变量的机制（与 Vue 不冲突，最稳）；`browser-compatibility` 的检测函数保留为工具，DOM 注入逻辑后续以组件形式重写。

**Tech Stack:** Pinia setup store、`spark-md5`（npm，已装）。`exif-js` 本阶段不用（utils 的 `window.extractExif` 是调试辅助，删除不迁；EXIF 真正使用在阶段 9 properties）。

**关于源码照搬：** 标注「照搬源码」的步骤，执行时读 `D:\repos\相册浏览器\js\<file>` 原样取数据/逻辑，只做去 global 化（加 `export`、全局符号改 `import`）。CONFIG 与 utils 函数代码本 plan 已完整给出（基于源码精读），可直接用。

**关于测试：** 纯逻辑（`FileTypes`/`format`）trivial 不自检；复杂逻辑（`calculateMD5` 前 2MB、`getImageInfoFromHeader` 魔数位运算）在后续阶段实际使用时自然验证（阶段 3 缩略图用 md5/魔数、阶段 5 排序用 compare）。**阶段 1 验收 = dev 能切 3 套主题且刷新后保持。**

**utils 拆分调整：** spec 原规划 `format/md5/mimetype/magic-bytes` 4 个文件，函数都很小，**合并为 `format.js` + `file.js`**（ponytail：少几个文件）。`browser-compat` 进 `utils/browser.js`。

---

## File Structure

| 文件 | 职责 | 来源 |
|---|---|---|
| `src/config/index.js` | `CONFIG` 常量 + `getConfig` 点分访问器 | config.js（CONFIG 无依赖，原样） |
| `src/config/file-types.js` | `FileTypes` 对象（getter + 方法，整体导出） | file-types.js（照搬，加 export） |
| `src/utils/format.js` | formatBytes/formatFileSize/formatDate/windowsCompareStrings/debounce | utils.js |
| `src/utils/file.js` | getMimeType/calculateMD5/getImageInfoFromHeader/verifyHandlePermission/convertToPngBlob | utils.js（calculateMD5 用 spark-md5） |
| `src/utils/browser.js` | getBrowserName/isFileSystemAccessSupported | browser-compatibility.js（仅纯检测函数） |
| `src/stores/userSettings.js` | Pinia：UserSettings（localStorage 持久化 + 响应式 settings） | config.js 的 UserSettings 类 |
| `src/stores/theme.js` | Pinia：THEMES 数据 + useThemeStore（applyTheme/init） | themes.js |

**不迁移：** `window.getPic`、`window.extractExif`（调试辅助，依赖外部 globals/EXIF）、`checkBrowserCompatibility`/`showIncompatibilityAlert` 的 DOM 注入逻辑（阶段 7 以 `BrowserUnsupportedWarning` 组件重写）。

---

## Task 1: config/（CONFIG + file-types）

**Files:**
- Create: `src/config/index.js`
- Create: `src/config/file-types.js`

- [ ] **Step 1: 写 src/config/index.js**

Create `src/config/index.js`:
```js
// 应用配置中心。搬自源码 js/config.js 的 CONFIG + getConfig,无外部依赖。
export const CONFIG = {
  DATABASE: {
    NAME: 'GalleryThumbnailDB',
    VERSION: 1,
    STORES: { THUMBNAILS: 'thumbnails' },
  },
  PERFORMANCE: {
    THUMBNAIL_QUEUE_SIZE: 4,
    INTERSECTION_MARGIN: '100px',
    DEBOUNCE_DELAY: 300,
  },
  UI: {
    MODAL: { MIN_SCALE: 0.1, MAX_SCALE: 10, ZOOM_STEP: 0.1, ZOOM_SENSITIVITY: 0.002 },
    SIDEBAR: { DEFAULT_WIDTH: 280, MIN_WIDTH: 200, MAX_WIDTH: 500 },
    TOAST: { DURATION: 3000, DURATION_LONG: 5000, MAX_VISIBLE: 3 },
  },
  DEFAULTS: {
    thumbnailSize: 400,
    columnCount: 5,
    sortField: 'name',
    sortDirection: 'asc',
    scrollZoneEnabled: true,
    scrollSpeed: 2.0,
    sidebarPinned: false,
    sidebarWidth: 280,
    theme: 'ocean',
  },
  CACHE: { OLD_THRESHOLD_DAYS: 20, AUTO_CLEANUP_ENABLED: false },
};

// 点分路径访问器,例:getConfig('UI.MODAL.MIN_SCALE')
export function getConfig(path) {
  const keys = path.split('.');
  let value = CONFIG;
  for (const key of keys) {
    value = value[key];
    if (value === undefined) {
      console.warn(`配置项不存在: ${path}`);
      return undefined;
    }
  }
  return value;
}
```

- [ ] **Step 2: 照搬源码 FileTypes 到 src/config/file-types.js**

读 `D:\repos\相册浏览器\js\file-types.js`，把整个 `FileTypes` 对象（含 `image/video/audio` 分组、`get all()`/`get allMedia()` getter、`getType/getDetailedType/isSupported` 方法）**原样复制**到 `src/config/file-types.js`，仅在前面加 `export`：

```js
// 文件类型表。搬自源码 js/file-types.js。
// 注意:FileTypes 用了 getter 和 this,使用方必须整体 import(FileTypes),不能解构。
export const FileTypes = {
  // ... 原样照搬源码整个对象 ...
};
```

**照搬要点（不得改动）：**
- `ogg` 同时在 video.common 和 audio.common——源码就这样，**别去重**。
- `getDetailedType` 里 video.common 和 audio.common 都返回 `'common'`——源码既有歧义，**照搬不修**。
- 所有 `this.image`/`this.video` 引用保持原样。

- [ ] **Step 3: Commit**

```bash
cd "d:/repos/相册浏览器-pwa" && git add src/config/ && git commit -m "feat(阶段1): config 模块(CONFIG + FileTypes)"
```

---

## Task 2: utils/（format + file + browser）

**Files:**
- Create: `src/utils/format.js`
- Create: `src/utils/file.js`
- Create: `src/utils/browser.js`

- [ ] **Step 1: 写 src/utils/format.js**

Create `src/utils/format.js`:
```js
// 格式化与排序工具。搬自源码 js/utils.js,无外部依赖。

export function formatBytes(bytes, decimals = 2) {
  if (bytes === 0) return '0 Bytes';
  const k = 1024;
  const sizes = ['Bytes', 'KB', 'MB', 'GB', 'TB'];
  const i = Math.floor(Math.log(bytes) / Math.log(k));
  return parseFloat((bytes / Math.pow(k, i)).toFixed(decimals)) + ' ' + sizes[i];
}

export function formatFileSize(bytes) {
  if (bytes === 0) return '0 B';
  const k = 1024;
  const sizes = ['B', 'KB', 'MB', 'GB'];
  const i = Math.floor(Math.log(bytes) / Math.log(k));
  return (bytes / Math.pow(k, i)).toFixed(2) + ' ' + sizes[i];
}

export function formatDate(ts) {
  return new Date(ts).toLocaleDateString();
}

// Windows 风格自然排序(数字按数值比较,中文 zh-CN)
export function windowsCompareStrings(a, b) {
  return a.localeCompare(b, 'zh-CN', { numeric: true, sensitivity: 'base' });
}

export function debounce(func, wait) {
  let timeout;
  return function (...args) {
    clearTimeout(timeout);
    timeout = setTimeout(() => func.apply(this, args), wait);
  };
}
```

> 注：`formatBytes`/`formatFileSize` 的 sizes 数组长度差异（5 vs 4）是源码既有，照搬不合并。

- [ ] **Step 2: 写 src/utils/file.js**

Create `src/utils/file.js`:
```js
// 文件处理工具。搬自源码 js/utils.js。
// calculateMD5 只 hash 前 2MB——必须保留此语义,否则与老版本 IndexedDB 缓存 key 不一致。
import SparkMD5 from 'spark-md5';

export function getMimeType(filename) {
  const ext = filename.split('.').pop().toLowerCase();
  const map = {
    jpg: 'image/jpeg', jpeg: 'image/jpeg', png: 'image/png',
    gif: 'image/gif', webp: 'image/webp', bmp: 'image/bmp', svg: 'image/svg+xml',
  };
  return map[ext] || 'application/octet-stream';
}

// 只 hash 前 2MB(性能换碰撞率)。chunkSize 必须保持 2097152,不可改全文件分片。
export function calculateMD5(file) {
  return new Promise((resolve, reject) => {
    const chunkSize = 2097152; // 2MB
    const chunk = file.slice(0, chunkSize);
    const fileReader = new FileReader();
    fileReader.onload = (e) => resolve(SparkMD5.ArrayBuffer.hash(e.target.result));
    fileReader.onerror = reject;
    fileReader.readAsArrayBuffer(chunk);
  });
}

// 文件头魔数识别,返回 [width, height, type] 或 null。位运算逐字符照搬源码,勿改。
export async function getImageInfoFromHeader(file) {
  if (file.size < 30) return null;
  let view = new DataView(await file.slice(0, 30).arrayBuffer());
  const sign = view.getUint32(0);

  if (sign === 0x89504e47) return [view.getUint32(16), view.getUint32(20), 'png'];
  if (sign === 0x47494638) return [view.getUint16(6, true), view.getUint16(8, true), 'gif'];
  if ((sign >>> 16) === 0x424d)
    return [Math.abs(view.getInt32(18, true)), Math.abs(view.getInt32(22, true)), 'bmp'];
  if ((sign >>> 8) === 0xffd8ff) {
    const jpegData = await file.slice(0, 128 * 1024).arrayBuffer();
    view = new DataView(jpegData);
    let offset = 2;
    while (offset < view.byteLength) {
      const marker = view.getUint16(offset);
      offset += 2;
      if (marker === 0xffc0 || marker === 0xffc2)
        return [view.getUint16(offset + 3), view.getUint16(offset + 1), 'jpg'];
      offset += view.getUint16(offset);
    }
  } else if (sign === 0x52494646) {
    view = new DataView(await file.slice(0, 40).arrayBuffer());
    const vp8 = view.getUint32(12);
    if (vp8 === 0x56503820) return [view.getUint16(26, true), view.getUint16(28, true), 'webp'];
    if (vp8 === 0x56503858)
      return [(view.getUint32(24, true) & 0x00ffffff) + 1, ((view.getUint32(27, true) >> 8) & 0x00ffffff) + 1, 'webp'];
    if (vp8 === 0x5650384c) {
      const b1 = view.getUint16(21, true);
      const b2 = view.getUint16(22, true);
      return [(b1 & 0x3fff) + 1, ((b2 >> 6) & 0x3fff) + 1, 'webp'];
    }
  }
  return null;
}

export async function verifyHandlePermission(handle) {
  if ((await handle.queryPermission({ mode: 'read' })) === 'granted') return true;
  if ((await handle.requestPermission({ mode: 'read' })) === 'granted') return true;
  throw new Error('权限被拒绝');
}

export function convertToPngBlob(blobUrl) {
  return new Promise((resolve, reject) => {
    const img = new Image();
    img.crossOrigin = 'Anonymous';
    img.onload = () => {
      try {
        const canvas = document.createElement('canvas');
        canvas.width = img.width;
        canvas.height = img.height;
        const ctx = canvas.getContext('2d');
        ctx.drawImage(img, 0, 0);
        canvas.toBlob((blob) => {
          if (blob) resolve(blob);
          else reject(new Error('Canvas 导出失败'));
        }, 'image/png', 1.0);
      } catch (e) {
        reject(e);
      }
    };
    img.onerror = () => reject(new Error('图片加载失败，无法转码'));
    img.src = blobUrl;
  });
}
```

> 注：源码 `calculateMD5` 用了 `File.prototype.slice || mozSlice || webkitSlice` 兼容写法，现代浏览器 `file.slice` 必然存在，已简化。

- [ ] **Step 3: 写 src/utils/browser.js**

Create `src/utils/browser.js`:
```js
// 浏览器兼容检测。搬自源码 js/browser-compatibility.js 的纯检测函数。
// DOM 注入逻辑(checkBrowserCompatibility/showIncompatibilityAlert)不迁,阶段 7 以组件重写。

export function getBrowserName() {
  const ua = navigator.userAgent;
  if (ua.includes('Firefox')) return 'Firefox';
  if (ua.includes('Edg')) return 'Microsoft Edge';   // 必须在 Chrome 之前判(Edge UA 含 Chrome)
  if (ua.includes('Chrome')) return 'Chrome';
  if (ua.includes('Safari')) return 'Safari';
  if (ua.includes('Opera') || ua.includes('OPR')) return 'Opera';
  return '未知浏览器';
}

export function isFileSystemAccessSupported() {
  return typeof window.showDirectoryPicker === 'function';
}
```

- [ ] **Step 4: 验证 build 通过（utils 被 main 间接引入前先确保语法/导入无误）**

Run:
```bash
cd "d:/repos/相册浏览器-pwa" && node --input-type=module -e "import('./src/config/file-types.js').then(m=>console.log('FileTypes image.all:', m.FileTypes.image.all.length, '| getType jpg:', m.FileTypes.getType('jpg'), '| getType mp4:', m.FileTypes.getType('mp4'))).catch(e=>console.error('ERR',e.message))"
```
Expected: 打印 `FileTypes image.all: 8 | getType jpg: image | getType mp4: video`（确认照搬的 FileTypes 方法可调）。

- [ ] **Step 5: Commit**

```bash
cd "d:/repos/相册浏览器-pwa" && git add src/utils/ && git commit -m "feat(阶段1): utils 模块(format/file/browser)"
```

---

## Task 3: stores/userSettings.js（Pinia）

**Files:**
- Create: `src/stores/userSettings.js`

- [ ] **Step 1: 写 userSettings store**

Create `src/stores/userSettings.js`:
```js
// 用户设置 store。搬自源码 js/config.js 的 UserSettings 静态类,改为 Pinia setup store。
// settings 响应式,UI 可直接绑定;load() 合并 CONFIG.DEFAULTS 保证新字段有默认值。
import { defineStore } from 'pinia';
import { ref } from 'vue';
import { CONFIG } from '../config';

const STORAGE_KEY = 'gallery-viewer-settings';

export const useUserSettingsStore = defineStore('userSettings', () => {
  const settings = ref({ ...CONFIG.DEFAULTS });

  function load() {
    try {
      const stored = localStorage.getItem(STORAGE_KEY);
      settings.value = stored
        ? { ...CONFIG.DEFAULTS, ...JSON.parse(stored) }
        : { ...CONFIG.DEFAULTS };
    } catch (e) {
      console.warn('加载用户设置失败:', e);
      settings.value = { ...CONFIG.DEFAULTS };
    }
  }

  function save() {
    localStorage.setItem(STORAGE_KEY, JSON.stringify(settings.value));
  }

  function get(key) {
    return settings.value[key];
  }

  function set(key, value) {
    settings.value = { ...settings.value, [key]: value };
    save();
  }

  function update(updates) {
    settings.value = { ...settings.value, ...updates };
    save();
  }

  function reset() {
    settings.value = { ...CONFIG.DEFAULTS };
    save();
  }

  // store 初始化即加载持久化设置
  load();

  return { settings, load, save, get, set, update, reset };
});
```

- [ ] **Step 2: Commit**

```bash
cd "d:/repos/相册浏览器-pwa" && git add src/stores/userSettings.js && git commit -m "feat(阶段1): userSettings Pinia store"
```

---

## Task 4: stores/theme.js（Pinia + THEMES 数据照搬）

**Files:**
- Create: `src/stores/theme.js`

- [ ] **Step 1: 照搬源码 THEMES 数据**

读 `D:\repos\相册浏览器\js\themes.js`，把 `THEMES` 对象（含 ocean/dark/forest 三套完整 `variables`）**原样复制**到 `src/stores/theme.js` 顶部并 `export`。三套主题的变量键**不一致**（dark 独有语义色 + text-inverse、forest 无 gray 系列）是源码现状，**照搬不对齐**。

```js
// 主题数据 + 主题 store。搬自源码 js/themes.js。
// applyTheme 保留 :root.style.setProperty 注入 CSS 变量的机制(与 Vue 不冲突,最稳)。
import { defineStore } from 'pinia';
import { ref } from 'vue';
import { useUserSettingsStore } from './userSettings';

export const THEMES = {
  ocean:  { id: 'ocean',  name: '海洋蓝', description: '清新的蓝色主题', icon: '🌊', variables: { /* 照搬源码 */ } },
  dark:   { id: 'dark',   name: '暗色模式', description: '舒适的深色主题，护眼模式', icon: '🌙', variables: { /* 照搬源码 */ } },
  forest: { id: 'forest', name: '森林绿', description: '清新的绿色主题', icon: '🌲', variables: { /* 照搬源码 */ } },
};

export const useThemeStore = defineStore('theme', () => {
  const currentTheme = ref('ocean');

  function applyTheme(themeId) {
    const theme = THEMES[themeId];
    if (!theme) { console.error(`主题不存在: ${themeId}`); return; }
    const root = document.documentElement;
    Object.entries(theme.variables).forEach(([key, value]) => {
      root.style.setProperty(key, value);
    });
    currentTheme.value = themeId;
    useUserSettingsStore().set('theme', themeId);
    document.body.setAttribute('data-theme', themeId);
  }

  function getThemes() {
    return Object.values(THEMES);
  }

  function getCurrentTheme() {
    return THEMES[currentTheme.value];
  }

  function init() {
    const saved = useUserSettingsStore().get('theme') || 'ocean';
    applyTheme(saved);
  }

  return { currentTheme, applyTheme, getThemes, getCurrentTheme, init };
});
```

**已知行为（照搬，不修）：** `applyTheme` 只 `setProperty` 不 `removeProperty`，切到变量更少的主题（forest）时上一个主题（dark）的 gray 系列会残留。这是源码既有行为。阶段 7 设置面板验收时若观感异常，再在 `applyTheme` 开头加 `root.style.cssText = ''` 清空 inline 变量（只清 inline，不影响 `variables.css` 样式表）。

- [ ] **Step 2: Commit**

```bash
cd "d:/repos/相册浏览器-pwa" && git add src/stores/theme.js && git commit -m "feat(阶段1): theme Pinia store + THEMES 数据"
```

---

## Task 5: App.vue 接入主题 + 临时切换 UI + 验收

**Files:**
- Modify: `src/App.vue`

- [ ] **Step 1: 改写 App.vue 接入主题 store + 临时切换 UI**

替换 `src/App.vue` 全部内容为：
```vue
<script setup>
import { onMounted } from 'vue';
import { useThemeStore } from './stores/theme';

const themeStore = useThemeStore();

onMounted(() => {
  // 应用持久化的主题
  themeStore.init();
});
</script>

<template>
  <div class="startup-placeholder">
    <i class="fas fa-images"></i>
    <h1>相册浏览器</h1>
    <p>骨架就绪 · 阶段 1:主题切换可用</p>

    <!-- 临时主题切换 UI(阶段 7 设置面板做好后替换) -->
    <div class="theme-switcher">
      <button
        v-for="t in themeStore.getThemes()"
        :key="t.id"
        :class="['theme-btn', { active: themeStore.currentTheme === t.id }]"
        @click="themeStore.applyTheme(t.id)"
      >
        <span class="icon">{{ t.icon }}</span>
        <span>{{ t.name }}</span>
      </button>
    </div>
  </div>
</template>

<style scoped>
.startup-placeholder {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 12px;
  background: var(--bg-secondary, #f5f7fa);
  color: var(--text-primary, #333);
}
.startup-placeholder i {
  font-size: 80px;
  color: var(--color-primary, #3498db);
}
.startup-placeholder h1 { font-size: 36px; margin: 0; }
.startup-placeholder p { color: var(--text-secondary, #666); }

.theme-switcher { display: flex; gap: 12px; margin-top: 24px; }
.theme-btn {
  display: inline-flex; align-items: center; gap: 8px;
  padding: 8px 16px; border: 2px solid var(--color-primary, #3498db);
  border-radius: 999px; background: var(--bg-primary, #fff);
  color: var(--text-primary, #333); cursor: pointer; font-size: 14px;
  transition: all 0.2s;
}
.theme-btn:hover { background: var(--color-primary, #3498db); color: #fff; }
.theme-btn.active { background: var(--color-primary, #3498db); color: #fff; }
.theme-btn .icon { font-size: 18px; }
</style>
```

- [ ] **Step 2: 验证 build 通过**

Run:
```bash
cd "d:/repos/相册浏览器-pwa" && npm run build 2>&1 | tail -8
```
Expected: 构建成功，无报错。

- [ ] **Step 3: Commit**

```bash
cd "d:/repos/相册浏览器-pwa" && git add src/App.vue && git commit -m "feat(阶段1): App.vue 接入主题 store + 临时切换 UI"
```

- [ ] **Step 4: 交付用户 dev 验收**

向用户报告阶段 1 完成的 5 个 task + 请 `npm run dev` 验收：
- 页面底部出现 3 个主题按钮（🌊海洋蓝 / 🌙暗色 / 🌲森林绿），当前主题高亮。
- 点击切换：主色（图标/按钮/active 态）、背景、文字颜色都跟着变。
- **刷新页面后主题保持**（localStorage 持久化生效）。
- 控制台无报错。

用户确认后 commit 阶段 1 收尾里程碑：
```bash
cd "d:/repos/相册浏览器-pwa" && git commit --allow-empty -m "chore: 阶段 1(配置基础)完成,用户 dev 验收通过"
```

---

## Self-Review（已执行）

1. **Spec 覆盖**：spec 第 8 节阶段 1 行（config/file-types/theme store/browser-compat/utils）均有对应 task。✓
2. **占位扫描**：无 TBD；FileTypes/THEMES 标注「照搬源码」是合理的已有代码引用，非占位；其余代码完整。✓
3. **命名一致**：`useUserSettingsStore`/`useThemeStore`、`CONFIG.DEFAULTS.theme`、`THEMES[id].variables` 跨 task 一致；`FileTypes` 整体导出与使用一致。✓
4. **关键语义保留**：calculateMD5 前 2MB（Task 2 Step 2）、UserSettings load 合并默认值（Task 3）、theme setProperty 机制（Task 4）均保留。✓
