# 阶段 6：单图预览 Modal Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans. Steps use checkbox (`- [ ]`) syntax.

**Goal:** 全屏预览（图片/视频/SVG/音频占位）：点击卡片打开 → 缩放（滚轮/捏合）/拖拽/翻页（方向键）/视频键盘控制/Ctrl+C 复制/ESC+点空白关闭，图片自适应屏幕。

**Architecture:**
- `stores/modal.js`（Pinia）：isOpen/currentIndex/currentFile/fileList + open(file,list)/openByIndex/next/prev/close。Gallery 点击时传 displayFiles，翻页自带。
- `composables/useModal.js`：手势（滚轮缩放以鼠标为中心+平移补偿 / 触摸捏合 / 鼠标+触摸拖拽 / Ctrl dragging-mode）+ 键盘（ESC/方向键翻页/视频悬停快进退+空格/Ctrl+C 复制）+ 图片自适应（initializeMediaDisplay）+ 复制（三 MIME）。scale/translate 用 **style 独立属性**。
- `components/MediaModal.vue`：Teleport to body + v-if isOpen + 模板分支渲染 img/video/svg(v-html)/audio(占位) + loader。
- Gallery/PhotoCard：点击卡片 → modalStore.open(file, displayFiles)。

**范围决策（子代理推荐，采纳）：**
- **做**：image(含GIF)/svg/video/audio占位 + 全部手势/键盘/复制/翻页/ESC/点空白/自适应/Teleport。
- **推迟阶段10**：LRU 缓存（先每次重建，浏览器 blobUrl 缓存秒开）、AudioPlayer.vue（ID3/可视化/进度）、文件级 handleFileNotFound（try/catch+console 占位）。

**Tech Stack:** Vue 3 Teleport、Pinia、Clipboard API、Touch API。

**关于测试：** 缩放/拖拽/触摸手感靠 dev 上手验收（用户偏好主观体验）。

---

## File Structure

| 文件 | 职责 |
|---|---|
| `src/stores/modal.js` | modal 状态 + open/next/prev/close（自带 fileList 翻页） |
| `src/composables/useModal.js` | 手势 + 键盘 + 复制 + 图片自适应（操作 modalStore + scale/pointX/pointY） |
| `src/components/MediaModal.vue` | Teleport + 模板分支渲染媒体 + loader |
| `src/components/PhotoCard.vue`（改） | emit click |
| `src/components/Gallery.vue`（改） | @click → modalStore.open(file, displayFiles) |
| `src/App.vue`（改） | 挂载 MediaModal |

---

## Task 1: stores/modal.js

**Files:**
- Create: `src/stores/modal.js`

- [ ] **Step 1: 写 modal store**

Create `src/stores/modal.js`:
```js
// Modal 状态 store。Gallery 点击时传 fileList(displayFiles),翻页自带,不依赖 Gallery 内部。
import { defineStore } from 'pinia';
import { ref } from 'vue';

export const useModalStore = defineStore('modal', () => {
  const isOpen = ref(false);
  const currentIndex = ref(-1);
  const currentFile = ref(null);
  const fileList = ref([]); // Gallery 传入的 displayFiles

  function open(file, list) {
    fileList.value = list;
    currentIndex.value = list.indexOf(file);
    currentFile.value = file;
    isOpen.value = true;
  }

  function openByIndex(i) {
    if (i < 0 || i >= fileList.value.length) return;
    currentIndex.value = i;
    currentFile.value = fileList.value[i];
  }

  function next() {
    if (currentIndex.value < fileList.value.length - 1) openByIndex(currentIndex.value + 1);
  }

  function prev() {
    if (currentIndex.value > 0) openByIndex(currentIndex.value - 1);
  }

  function close() {
    isOpen.value = false;
    currentIndex.value = -1;
    currentFile.value = null;
  }

  return { isOpen, currentIndex, currentFile, fileList, open, openByIndex, next, prev, close };
});
```

- [ ] **Step 2: Commit**

```bash
cd "d:/repos/相册浏览器-pwa" && git add src/stores/modal.js && git commit -m "feat(阶段6): modal Pinia store(自带 fileList 翻页)"
```

---

## Task 2: composables/useModal.js（手势+键盘+复制+自适应）

**Files:**
- Create: `src/composables/useModal.js`

- [ ] **Step 1: 写 useModal.js**

Create `src/composables/useModal.js`:
```js
// Modal 交互 composable。搬自源码 js/modal.js 的手势/键盘/复制 + events.js 的翻页/视频键盘。
// scale/translate 用 style 独立属性(源码如此,浏览器更优路径)。LRU 缓存推迟阶段10。
import { ref, computed, watch, onMounted, onBeforeUnmount, nextTick } from 'vue';
import { useModalStore } from '../stores/modal.js';
import { useFsStore } from '../stores/fs.js';
import { CONFIG } from '../config/index.js';
import { FileTypes } from '../config/file-types.js';
import { convertToPngBlob } from '../utils/file.js';

const { MIN_SCALE, MAX_SCALE, ZOOM_STEP } = CONFIG.UI.MODAL;

// mediaKind 分类(image/svg/video/audio)
function getMediaKind(type) {
  if (FileTypes.image.svg.includes(type)) return 'svg';
  if (FileTypes.video.all.includes(type)) return 'video';
  if (FileTypes.audio.all.includes(type)) return 'audio';
  return 'image'; // standard + gif
}

export function useModal(modalElRef, contentElRef, mediaElRef) {
  const modal = useModalStore();
  const fs = useFsStore();

  const scale = ref(1);
  const pointX = ref(0);
  const pointY = ref(0);
  const minScale = ref(MIN_SCALE);
  const isHoveringVideo = ref(false);
  const loading = ref(false);
  const svgText = ref('');

  const mediaKind = computed(() => (modal.currentFile ? getMediaKind(modal.currentFile.type) : null));

  // 拖拽临时状态(非响应式)
  let panning = false;
  let startX = 0, startY = 0;
  let mouseDownTime = 0, mouseDownX = 0, mouseDownY = 0;
  let initialDistance = 0, initialScale = 1;

  function applyTransform() {
    const el = mediaElRef.value;
    if (!el) return;
    el.style.scale = scale.value;
    el.style.translate = `${pointX.value}px ${pointY.value}px`;
  }

  function resetTransform() {
    scale.value = 1;
    pointX.value = 0;
    pointY.value = 0;
    applyTransform();
  }

  // 图片自适应屏幕:设 natural 像素 + scale 适应 + minScale=初始
  function initializeMediaDisplay() {
    const el = mediaElRef.value;
    if (!el || !el.classList.contains('modal-image')) return;
    el.style.width = `${el.naturalWidth}px`;
    el.style.height = `${el.naturalHeight}px`;
    const vw = window.innerWidth, vh = window.innerHeight;
    const initial = Math.min(vh / el.naturalHeight, vw / el.naturalWidth, 1) * 0.99;
    scale.value = initial;
    pointX.value = 0;
    pointY.value = 0;
    minScale.value = initial;
    applyTransform();
  }

  function onImgLoad() {
    loading.value = false;
    initializeMediaDisplay();
  }

  // ===== 滚轮缩放(以鼠标为中心 + 平移补偿) =====
  function onWheel(e) {
    if (!modal.isOpen) return;
    e.preventDefault();
    const delta = e.deltaY > 0 ? -1 : 1;
    const ratio = 1 + delta * ZOOM_STEP;
    const newScale = scale.value * ratio;
    if (newScale < minScale.value || newScale > MAX_SCALE) return;
    const el = mediaElRef.value;
    if (!el) return;
    const rect = el.getBoundingClientRect();
    const offsetX = e.clientX - rect.left - rect.width / 2;
    const offsetY = e.clientY - rect.top - rect.height / 2;
    pointX.value = pointX.value - offsetX * (ratio - 1);
    pointY.value = pointY.value - offsetY * (ratio - 1);
    scale.value = newScale;
    applyTransform();
  }

  // ===== 鼠标拖拽 =====
  function onMouseDown(e) {
    if (!modal.isOpen || e.button !== 0) return;
    e.preventDefault();
    panning = true;
    startX = e.clientX - pointX.value;
    startY = e.clientY - pointY.value;
    mouseDownTime = Date.now();
    mouseDownX = e.clientX;
    mouseDownY = e.clientY;
    if (modalElRef.value) modalElRef.value.style.cursor = 'grabbing';
  }

  function onMouseMove(e) {
    if (!panning || !modal.isOpen) return;
    e.preventDefault();
    const moveX = e.clientX - mouseDownX;
    const moveY = e.clientY - mouseDownY;
    if (Math.sqrt(moveX * moveX + moveY * moveY) > 5) {
      pointX.value = e.clientX - startX;
      pointY.value = e.clientY - startY;
      applyTransform();
    }
  }

  function onMouseUp(e) {
    if (!panning) return;
    const dur = Date.now() - mouseDownTime;
    const moveX = e.clientX - mouseDownX;
    const moveY = e.clientY - mouseDownY;
    const dist = Math.sqrt(moveX * moveX + moveY * moveY);
    const isClick = dist < 5 && dur < 300;
    if (isClick) {
      // 点空白(非媒体/非音频播放器)关闭
      const onMedia = e.target.closest('.modal-media, audio, .modal-audio-player');
      if (!onMedia) modal.close();
    }
    panning = false;
    if (modalElRef.value) modalElRef.value.style.cursor = '';
  }

  // ===== 触摸 =====
  function onTouchStart(e) {
    if (!modal.isOpen) return;
    if (e.touches.length === 1) {
      panning = true;
      startX = e.touches[0].clientX - pointX.value;
      startY = e.touches[0].clientY - pointY.value;
      mouseDownX = e.touches[0].clientX;
      mouseDownY = e.touches[0].clientY;
      mouseDownTime = Date.now();
    } else if (e.touches.length === 2) {
      e.preventDefault();
      const dx = e.touches[0].clientX - e.touches[1].clientX;
      const dy = e.touches[0].clientY - e.touches[1].clientY;
      initialDistance = Math.sqrt(dx * dx + dy * dy);
      initialScale = scale.value;
    }
  }

  function onTouchMove(e) {
    if (!modal.isOpen) return;
    if (e.touches.length === 1 && panning) {
      const moveX = e.touches[0].clientX - mouseDownX;
      const moveY = e.touches[0].clientY - mouseDownY;
      if (Math.sqrt(moveX * moveX + moveY * moveY) > 5) {
        pointX.value = e.touches[0].clientX - startX;
        pointY.value = e.touches[0].clientY - startY;
        applyTransform();
      }
    } else if (e.touches.length === 2) {
      e.preventDefault();
      const dx = e.touches[0].clientX - e.touches[1].clientX;
      const dy = e.touches[0].clientY - e.touches[1].clientY;
      const cur = Math.sqrt(dx * dx + dy * dy);
      const newScale = initialScale * (cur / initialDistance);
      if (newScale < MIN_SCALE || newScale > MAX_SCALE) return;
      scale.value = newScale;
      applyTransform();
    }
  }

  function onTouchEnd(e) {
    if (e.touches.length > 0) return; // 还有手指
    if (!panning) return;
    const dur = Date.now() - mouseDownTime;
    const moveX = mouseDownX; // touchend 无 touches,用记录值近似(已移动则 mouseDownX 已是起点)
    const dist = 0; // 简化:touchend 判 tap 用时长
    if (dur < 300) {
      const onMedia = e.target.closest('.modal-media, audio, .modal-audio-player');
      if (!onMedia) modal.close();
    }
    panning = false;
  }

  // ===== 键盘 =====
  function onKeydown(e) {
    if (!modal.isOpen) return;
    const tag = document.activeElement?.tagName;
    if (tag === 'INPUT' || tag === 'TEXTAREA') return;

    // 视频悬停:方向键快进退 + 空格暂停
    if (isHoveringVideo.value) {
      const video = mediaElRef.value;
      if (video && video.tagName === 'VIDEO') {
        if (e.key === 'ArrowLeft' || e.key === 'ArrowRight') {
          e.preventDefault();
          const t = e.key === 'ArrowRight' ? 5 : -5;
          video.currentTime = Math.max(0, Math.min(video.currentTime + t, video.duration));
          return;
        }
        if (e.key === ' ' || e.key === 'Spacebar') {
          e.preventDefault();
          video.paused ? video.play() : video.pause();
          return;
        }
      }
    }

    switch (e.key) {
      case 'Escape':
        modal.close();
        break;
      case 'ArrowRight':
        modal.next();
        break;
      case 'ArrowLeft':
        modal.prev();
        break;
      case 'c':
      case 'C':
        if (e.ctrlKey || e.metaKey) {
          e.preventDefault();
          copyCurrent();
        }
        break;
    }
  }

  // ===== Ctrl+C 复制(三 MIME:image/png + text/plain + text/html) =====
  async function copyCurrent() {
    const file = modal.currentFile;
    if (!file || getMediaKind(file.type) !== 'image') {
      console.warn('仅支持复制图片');
      return;
    }
    try {
      const raw = await file.handle.getFile();
      const targetBlob = raw.type === 'image/png' ? raw : await convertToPngBlob(file.blobUrl);
      if (!targetBlob) throw new Error('无法生成图片数据');
      const item = new ClipboardItem({
        'image/png': targetBlob,
        'text/plain': new Blob([file.blobUrl], { type: 'text/plain' }),
        'text/html': new Blob([`<img src="${file.blobUrl}" alt="${file.name}" />`], { type: 'text/html' }),
      });
      await navigator.clipboard.write([item]);
      console.log(`已复制: ${file.name}`);
    } catch (err) {
      console.error('复制失败:', err);
    }
  }

  // ===== SVG:fetch blobUrl → text =====
  async function loadSvg() {
    if (mediaKind.value !== 'svg' || !modal.currentFile) return;
    try {
      const r = await fetch(modal.currentFile.blobUrl);
      svgText.value = await r.text();
    } catch (e) {
      console.warn('SVG 加载失败:', e);
    }
  }

  // 切换文件时重置变换 + 按需加载 svg
  watch(
    () => modal.currentFile,
    async (f) => {
      if (!f) return;
      resetTransform();
      loading.value = mediaKind.value !== 'audio';
      svgText.value = '';
      if (mediaKind.value === 'svg') await loadSvg();
    },
  );

  // isOpen 变化时挂/卸键盘
  watch(
    () => modal.isOpen,
    (open) => {
      if (open) {
        window.addEventListener('keydown', onKeydown);
        nextTick(() => resetTransform());
      } else {
        window.removeEventListener('keydown', onKeydown);
      }
    },
  );

  // 手势监听挂 modalEl(passive:false 才能 preventDefault)
  onMounted(() => {
    const el = modalElRef.value;
    if (!el) return;
    el.addEventListener('wheel', onWheel, { passive: false });
    el.addEventListener('mousedown', onMouseDown);
    el.addEventListener('touchstart', onTouchStart, { passive: false });
    el.addEventListener('touchmove', onTouchMove, { passive: false });
    el.addEventListener('touchend', onTouchEnd);
    window.addEventListener('mousemove', onMouseMove);
    window.addEventListener('mouseup', onMouseUp);
  });

  onBeforeUnmount(() => {
    const el = modalElRef.value;
    if (el) {
      el.removeEventListener('wheel', onWheel);
      el.removeEventListener('mousedown', onMouseDown);
      el.removeEventListener('touchstart', onTouchStart);
      el.removeEventListener('touchmove', onTouchMove);
      el.removeEventListener('touchend', onTouchEnd);
    }
    window.removeEventListener('mousemove', onMouseMove);
    window.removeEventListener('mouseup', onMouseUp);
    window.removeEventListener('keydown', onKeydown);
  });

  return {
    scale, pointX, pointY, minScale, isHoveringVideo, loading, svgText, mediaKind,
    applyTransform, resetTransform, initializeMediaDisplay, onImgLoad, copyCurrent,
  };
}
```

> 注：MediaModal 的 `:style="{ scale, translate }"` 不用——这里 applyTransform 直接操作 `mediaEl.style`（和源码一致，避免 Vue 对 scale/translate 独立属性的响应式开销）。`mediaElRef` 由 MediaModal 传。

- [ ] **Step 2: Commit**

```bash
cd "d:/repos/相册浏览器-pwa" && git add src/composables/useModal.js && git commit -m "feat(阶段6): useModal composable(手势+键盘+复制+图片自适应)"
```

---

## Task 3: components/MediaModal.vue

**Files:**
- Create: `src/components/MediaModal.vue`

- [ ] **Step 1: 写 MediaModal.vue**

Create `src/components/MediaModal.vue`:
```vue
<script setup>
import { ref } from 'vue';
import { useModalStore } from '../stores/modal.js';
import { useModal } from '../composables/useModal.js';

const modal = useModalStore();
const modalEl = ref(null);
const contentEl = ref(null);
const mediaEl = ref(null);

const {
  loading, svgText, mediaKind, onImgLoad, copyCurrent,
} = useModal(modalEl, contentEl, mediaEl);
</script>

<template>
  <Teleport to="body">
    <div v-if="modal.isOpen" class="modal" ref="modalEl">
      <div class="modal-content" ref="contentEl">
        <img
          v-if="mediaKind === 'image'"
          ref="mediaEl"
          class="modal-media modal-image"
          :src="modal.currentFile.blobUrl"
          draggable="false"
          alt="Full view"
          @load="onImgLoad"
        />
        <video
          v-else-if="mediaKind === 'video'"
          ref="mediaEl"
          class="modal-media modal-video"
          :src="modal.currentFile.blobUrl"
          controls
        />
        <div
          v-else-if="mediaKind === 'svg'"
          ref="mediaEl"
          class="modal-media svg-container"
          v-html="svgText"
        ></div>
        <audio
          v-else-if="mediaKind === 'audio'"
          :src="modal.currentFile.blobUrl"
          controls
          autoplay
        ></audio>
      </div>
      <div v-if="loading" class="loader">
        <i class="fas fa-spinner fa-spin"></i>
      </div>
    </div>
  </Teleport>
</template>
```

> 注：媒体元素 class 带 `modal-media`（applyTransform/点击判定靠它）；img @load 触发自适应；svg 用 v-html 渲染 fetch 到的文本；audio 占位（阶段 10 换 AudioPlayer）。

- [ ] **Step 2: Commit**

```bash
cd "d:/repos/相册浏览器-pwa" && git add src/components/MediaModal.vue && git commit -m "feat(阶段6): MediaModal 组件(Teleport+img/video/svg/audio 占位)"
```

---

## Task 4: PhotoCard emit click + Gallery 触发 + App 挂载 + 验收

**Files:**
- Modify: `src/components/PhotoCard.vue`（emit click）
- Modify: `src/components/Gallery.vue`（@click → modalStore.open）
- Modify: `src/App.vue`（挂 MediaModal）

- [ ] **Step 1: PhotoCard emit click**

Edit `src/components/PhotoCard.vue`：`defineProps` 后加 `defineEmits(['click'])`，模板根 `.photo-card` 加 `@click="$emit('click')"`。

`<script setup>` 加：
```js
defineEmits(['click']);
```

`<template>` 的 `<div class="photo-card" draggable="true">` 改为：
```html
<div class="photo-card" draggable="true" @click="$emit('click')">
```

- [ ] **Step 2: Gallery 触发 modal open**

Edit `src/components/Gallery.vue`：

`<script setup>` import + openModal：
```js
import { useModalStore } from '../stores/modal.js';
const modal = useModalStore();
function openPreview(file) {
  modal.open(file, displayFiles.value);
}
```

模板 `<PhotoCard>` 加 `@click="openPreview"`：
```html
<PhotoCard
  v-for="f in col"
  :key="f.path"
  :file="f"
  :target-size="settings.settings.thumbnailSize"
  @click="openPreview(f)"
/>
```

- [ ] **Step 3: App 挂 MediaModal**

Edit `src/App.vue`：

`<script setup>` import：
```js
import MediaModal from './components/MediaModal.vue';
```

`<template>` 在 `.app-root` 内末尾（`</div>` 前）加：
```html
<MediaModal />
```

- [ ] **Step 4: 验证 build 通过**

Run:
```bash
cd "d:/repos/相册浏览器-pwa" && npm run build 2>&1 | tail -8
```
Expected: 构建成功。

- [ ] **Step 5: Commit**

```bash
cd "d:/repos/相册浏览器-pwa" && rm -rf dist && git add src/components/PhotoCard.vue src/components/Gallery.vue src/App.vue && git commit -m "feat(阶段6): PhotoCard 点击触发 modal + Gallery openPreview + App 挂 MediaModal"
```

- [ ] **Step 6: 交付用户 dev 验收**

向用户报告阶段 6 完成 + 请 `npm run dev` 验收：
1. **点击任意卡片** → 全屏 Modal 打开（图片自适应屏幕）。
2. **滚轮缩放**：以鼠标位置为中心放大/缩小（不能小到看不见，minScale 限制）。
3. **拖拽平移**：按住拖动图片；点空白（<5px 移动 + <300ms）关闭。
4. **方向键翻页**：← → 切上/下一张（modal 自带 fileList）。
5. **ESC 关闭**。
6. **视频**：点视频卡片 → modal 显示 video + 控制条；鼠标悬停视频时，方向键改快进/退 5 秒、空格暂停/播放。
7. **Ctrl+C 复制**：在 modal 里 Ctrl+C → 粘贴到聊天/画图能出图。
8. **SVG**：点 svg 卡片 → modal 内联渲染 svg。
9. **音频**：点音频卡片 → modal 里 `<audio>` 播放器（占位，阶段 10 换 AudioPlayer）。
10. 控制台无报错。

用户确认后 commit 阶段 6 里程碑：
```bash
cd "d:/repos/相册浏览器-pwa" && git commit --allow-empty -m "chore: 阶段 6(单图预览 Modal)完成,用户 dev 验收通过"
```

---

## Self-Review（已执行）

1. **Spec 覆盖**：spec 阶段 6（MediaModal + useModal 缩放/拖拽/触摸/LRU）覆盖；LRU 明确推迟阶段10。✓
2. **占位扫描**：modalStore/useModal/MediaModal/PhotoCard/Gallery/App 代码完整。✓
3. **scale/translate**：用 style 独立属性（applyTransform 直接操作 el.style），非 transform。✓
4. **范围推迟**：LRU、AudioPlayer、文件级 handleFileNotFound 明确推迟。✓
5. **接口一致**：modalStore.open(file, list)/next/prev/close、useModal(modalEl, contentEl, mediaEl)、PhotoCard emit click 跨 task 一致。✓
6. **手势 passive:false**：wheel/touchstart/touchmove 需 preventDefault，onMounted 用 { passive: false }。✓
