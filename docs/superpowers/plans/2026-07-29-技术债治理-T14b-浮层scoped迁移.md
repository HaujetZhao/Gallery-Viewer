# T14b · 浮层 scoped 迁移(context-menu / confirm-dialog / properties)

> **For agentic workers:** REQUIRED SUB-SKILL: 用 `superpowers:executing-plans` 或 `subagent-driven-development` 按步执行。步骤用 `- [ ]` 跟踪。
> **父规划:** [2026-07-29-技术债治理-总规划.md](2026-07-29-技术债治理-总规划.md)
> ⚠️ **基于 master(含 T14a)**:开工前确认 `debt/T14a` 已合并 master。

**Goal:** 把 3 个「1:1 对应」的浮层 CSS 整体搬进各自组件 `<style scoped>`,**视觉零变化**(纯搬家 + scoped 隔离)。

**3 个浮层(CSS 文件 ↔ 组件 1:1,简单):**
- [context-menu.css](../../../src/styles/context-menu.css)(84 行)→ [ContextMenu.vue](../../../src/components/ContextMenu.vue) scoped
- [confirm-dialog.css](../../../src/styles/confirm-dialog.css)(278 行)→ [ConfirmDialog.vue](../../../src/components/ConfirmDialog.vue) scoped
- [properties.css](../../../src/styles/properties.css)(256 行)→ [PropertiesPanel.vue](../../../src/components/PropertiesPanel.vue) scoped(并入现有 scoped 块)

**策略(同 T14a):** 纯搬家,保留 `var(--*)` + T10 的 `[data-theme="dark"]` 嵌套块 + z-index 令牌(`var(--z-*)`)。不强换 utility。

**Tech Stack:** Vue 3 scoped CSS

---

## 一、归属核实(执行者动手前 grep 确认 1:1)

- [x] **Step 1:核实每个 CSS 文件的选择器都只被对应组件用**

对 context-menu.css / confirm-dialog.css / properties.css 的每个 `.xxx` 选择器,grep 全项目确认**只在对应组件 template 出现**(无跨组件共用)。若发现某 class 被别的组件用(跨组件),**别盲搬**(那需留全局或 :deep),反馈架构师。
```bash
# 例:context-menu.css 的选择器
grep -rn "context-menu\|context-menu-item\|context-menu-divider" src/components/
```
(预期:只 ContextMenu.vue 命中。confirm-dialog / properties 同理。)

## 二、ContextMenu.vue 加 scoped

- [x] **Step 2:context-menu.css 整文件搬入 ContextMenu.vue `<style scoped>`**

ContextMenu.vue 末尾加 `<style scoped>`,把 context-menu.css 全部内容(含 T10 的 `[data-theme="dark"]` 嵌套块 + `z-index: var(--z-popover)`)原样搬入。删 context-menu.css。main.css 删 `@import 'context-menu.css'`。

## 三、ConfirmDialog.vue 加 scoped

- [x] **Step 3:confirm-dialog.css 整文件搬入 ConfirmDialog.vue `<style scoped>`**

同上。confirm-dialog.css(含 `[data-theme="dark"]` 块 + `z-index: var(--z-confirm)` + 进场缩放动画)搬入 ConfirmDialog.vue scoped。删 confirm-dialog.css + main.css @import。

## 四、PropertiesPanel.vue 合并 scoped

- [x] **Step 4:properties.css 搬入 PropertiesPanel.vue 现有 `<style scoped>`**

PropertiesPanel.vue 已有 scoped 块(T14a grep 确认 L217)。把 properties.css 全部内容(含 `[data-theme="dark"]` 块)搬入该 scoped 块(合并;若现有 scoped 有残留重命名样式,T12 已清)。删 properties.css + main.css @import。

---

## 五、scoped + T10 dark 块的关键验证

- [x] **Step 5:dark 模式视觉验证(关键)**

T10 的 `[data-theme="dark"] { .child {} }` 嵌套块搬进 scoped 后:scoped 把 data-v 加到 `.child`(组件元素),`[data-theme="dark"]`(body 属性,组件外)不加 data-v → 编译为 `[data-theme="dark"] .child[data-v]`,**body[data-theme=dark] 下的组件元素仍匹配**。但**必须 dark 模式实测验证**(嵌套 + scoped + lightningcss 编译顺序有边角):
- 切到 dark 主题 → 这 3 个浮层(右键菜单 / 确认框 / 属性面板)的暗色样式正常应用。
- 若 dark 样式丢失(嵌套+scoped 编译问题),反馈架构师(备选:把嵌套展平为 `[data-theme="dark"] .child` 扁平选择器,scoped 仍匹配)。

## 六、验收

- [x] **Step V.1:视觉零变化(亮 + dark 双模式)**
右键菜单 / 确认框(两步)/ 属性面板(EXIF/ID3/GPS)→ 亮色 + dark 主题下都与 T14b 前一致。
- [x] **Step V.2:双 build + 测试 + Lint**
```bash
rm -rf dist && npm run build
rm -rf dist && npm run build:pwa
npm test && npm run lint
```
- [x] **Step V.3:grep 确认**
```bash
ls src/styles/context-menu.css src/styles/confirm-dialog.css src/styles/properties.css  # 全不存在
grep -n "context-menu.css\|confirm-dialog.css\|properties.css" src/styles/main.css  # 零
```

---

## 七、验收点

**客观断言:**
- 3 个 CSS 文件删除;main.css 不再 @import;ContextMenu/ConfirmDialog 加 scoped、PropertiesPanel scoped 含 properties.css 内容;四绿。

**主观体验(产品负责人,亮+dark 双模式逐项):**
- 右键菜单 / 确认框(两步进度)/ 属性面板 → 亮 + dark 都与 T14b 前一致(dark 下暗色样式不丢)。

---

## 八、变更技术报告(执行者完成后填写)

```
## 变更技术报告 — T14b

### 改了什么
- [x] ContextMenu.vue 加 scoped(搬 context-menu.css:含 [data-theme="dark"] 嵌套块 + z-index: var(--z-popover))
- [x] ConfirmDialog.vue 加 scoped(搬 confirm-dialog.css:含 [data-theme="dark"] 块 + z-index: var(--z-confirm) + pulse/shake @keyframes + 进场缩放)
- [x] PropertiesPanel.vue 合并 scoped(搬 properties.css:含 [data-theme="dark"] 块;并入现有 T12 .props-filename/.props-rename-icon 块,未建第二 scoped)
- [x] 删 3 CSS(context-menu/confirm-dialog/properties)+ 清 import

### ⚠️ 偏离文档(已纠正)
- 加载方式与文档不符:文档 Step 2/3/V.3 称"context-menu/confirm-dialog 是 main.css 的 @import"。**实际是 src/main.js:8-9 直接 import**(注释明示"源码是单独 link,这里补上")。故删的是 main.js 两行 import,**不是** main.css。properties.css 才是 main.css @import(已删 main.css:9)。验收 V.3 grep 已据此查 main.js + main.css 两处。

### 涉及文件
- src/components/ContextMenu.vue(+87 行 scoped)
- src/components/ConfirmDialog.vue(+281 行 scoped)
- src/components/PropertiesPanel.vue(+257 行并入现有 scoped)
- src/styles/context-menu.css(删 -85)、confirm-dialog.css(删 -279)、properties.css(删 -257)
- src/main.js(-2 行 import + 注释修订)、src/styles/main.css(-1 行 @import)

### dark 块 scoped 验证(关键风险点)
- [data-theme="dark"] 由 src/stores/theme.js:119 document.body.setAttribute('data-theme', themeId) 设在 **body**。
- scoped 把 data-v 加到组件元素(3 浮层均 Teleport to body,data-v 编译期注入、Teleport 后仍保留);body[data-theme=dark] 在组件外不加 data-v → 编译为 [data-theme="dark"] .child[data-v],body[data-theme=dark] 下组件元素仍匹配。嵌套块逻辑成立。
- ⚠️ 静态论证成立,但 scoped + CSS nesting + lightningcss 编译顺序有边角,**必须 dark 模式实测**(切 dark → 右键菜单/确认框/属性面板暗色样式)。若丢失,备选:嵌套展平为 [data-theme="dark"] .child 扁平选择器。**待产品负责人 dark 模式主观验收。**

### 归属核实(Step 1)
- context-menu 选择器(.context-menu/.context-menu-item/.context-menu-divider):**只 ContextMenu.vue 命中**。PhotoCard.vue:136/143 与 sidebar.css:163 命中的是 .context-menu-active(卡片/树节点右键激活态,独立 class,不在 context-menu.css),不受影响。
- confirm-dialog 选择器:只 ConfirmDialog.vue。properties 选择器:只 PropertiesPanel.vue。**无跨组件共用 class,纯搬家安全,未用 :deep。**

### 验收基线(全绿)
- lint ✅  test ✅(15 文件 / 95 用例)  build ✅(101 modules,单 HTML 内联)  build:pwa ✅(precache 14 entries)
- grep 确认:3 CSS 已删;main.css 零 properties.css;main.js 零 context-menu/confirm-dialog.css

### 视觉零回归(产品负责人,亮+dark 双模式逐项 —— 待主观验收)
- 右键菜单 / 确认框(两步进度)/ 属性面板(EXIF/ID3/GPS 地图按钮):待产品负责人浏览器上手,亮+dark 双模式逐项核对(**dark 下暗色样式不丢是硬指标**)

### 遗留 / 风险 / 偏离
- 加载方式偏离文档(已纠正为删 main.js import)。
- dark 嵌套块 + scoped 编译:静态论证成立,实测待主观验收(最大风险点)。
- .hidden 死代码选择器(.context-menu.hidden/.confirm-dialog-overlay.hidden/.confirm-btn.hidden):组件改用 v-if 显隐,这些选择器不匹配、无害,纯搬家原样保留未删。
- 未提交 git(等产品负责人主观验收后)。
```

---

## 九、执行者注意

1. **先 grep 核实 1:1**(Step 1)——发现跨组件共用 class 别盲搬,反馈。
2. **纯搬家**——var(--*)/dark 块/z-index 令牌/动画原样搬,不改。
3. **dark 块是 T14b 最大风险点**——`[data-theme="dark"]` 嵌套 + scoped + lightningcss 编译顺序,必须 dark 模式实测;丢失则展平选择器(扁平 `[data-theme="dark"] .child`)。
4. **PropertiesPanel 已有 scoped**(T14a grep L217),合并进现有块(别建两个 scoped)。
5. 视觉零回归(亮+dark)是硬指标。
6. 双 build 分别 `rm -rf dist`。
