# 阶段 0：工程骨架 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 `d:\repos\相册浏览器-pwa` 立起 Vue 3 + Vite 双 build 骨架——`npm run dev` 能跑出挂载成功的空页，`npm run build` 出单 HTML，`npm run build:pwa` 出完整 PWA 产物。

**Architecture:** 照搬 SubTap 模板的双 config 模式——`vite.config.js`(singlefile) + `vite.config.pwa.js`(vite-plugin-pwa)，都设 `base: './'`。资源全内化：font-awesome/SparkMD5/exif-js 装 npm 包走 ES import，Vite 自动接管字体资源。源码 11 个 CSS 原样搬到 `src/styles/` 全局 import。Pinia 接入但阶段 0 暂不建 store。

**Tech Stack:** Vue ^3.4, Vite ^5, Pinia ^2, vite-plugin-singlefile ^2, vite-plugin-pwa ^0.20, @fortawesome/fontawesome-free ^7, @resvg/resvg-js ^2（仅图标生成用 devDep）。

**关于测试（重要）：** 阶段 0 是纯配置/搭建，**无可单测的逻辑单元**（spec 第 7.4 决策：暂不加测试框架）。验收用「构建成功 + 产物机械核对 + 用户 dev 上手」三件套，**不写单测**。复杂纯逻辑的自检从阶段 2（数据模型）起才出现。

**执行约定：**
- `npm install`、`npm run build`、`npm run build:pwa`、`node scripts/...`、`git` 由 Claude 跑。
- `npm run dev` 与浏览器上手验收由**用户**做（用户偏好：UI 体验验收自己来）。
- 所有文件路径相对工作区根 `d:\repos\相册浏览器-pwa`。

---

## File Structure

阶段 0 创建的文件及其职责：

| 文件 | 职责 |
|---|---|
| `package.json` | 依赖声明 + scripts（dev/build/build:pwa/preview/icons） |
| `vite.config.js` | 单 HTML build 配置（singlefile 内联一切） |
| `vite.config.pwa.js` | PWA build 配置（SW + manifest） |
| `index.html` | Vue 入口：`#app` + inline SVG favicon + title |
| `src/main.js` | createApp + Pinia + 全局 CSS + font-awesome import |
| `src/App.vue` | 骨架空壳（证明挂载 + 字体内化 + 设计变量生效） |
| `src/styles/*.css` | 从源码 `D:\repos\相册浏览器\css\` 搬来的 11 个文件 |
| `public/icon.svg` | 应用图标源（从源码 dist 搬来） |
| `public/icon-192.png` / `icon-512.png` | PWA 图标（由脚本从 svg 生成，占位） |
| `scripts/gen-icons.mjs` | svg→PNG 渲染脚本（用 @resvg/resvg-js） |

---

## Task 1: package.json + git user + 安装依赖

**Files:**
- Create: `package.json`

- [ ] **Step 1: 配置仓库级 git user**（让后续 commit 不必每次带 `-c`）

Run:
```bash
cd "d:/repos/相册浏览器-pwa" && git config user.name "Haujet" && git config user.email "haujet@local" && git config user.name && git config user.email
```
Expected: 输出两行 `Haujet` / `haujet@local`

- [ ] **Step 2: 写 package.json**

Create `package.json`:
```json
{
  "name": "xiangce-browser-pwa",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "build:pwa": "vite build --config vite.config.pwa.js",
    "preview": "vite preview",
    "icons": "node scripts/gen-icons.mjs"
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^7.3.1",
    "exif-js": "^2.3.0",
    "pinia": "^2.2.0",
    "spark-md5": "^3.0.2",
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.0",
    "@vitejs/plugin-vue": "^5.0.0",
    "vite": "^5.0.0",
    "vite-plugin-pwa": "^0.20.0",
    "vite-plugin-singlefile": "^2.0.0"
  }
}
```

- [ ] **Step 3: 安装依赖**

Run:
```bash
cd "d:/repos/相册浏览器-pwa" && npm install
```
Expected: 安装完成，无报错（npm warning 可忽略）。生成 `node_modules/` 与 `package-lock.json`。

- [ ] **Step 4: 验证关键依赖到位**

Run:
```bash
cd "d:/repos/相册浏览器-pwa" && ls node_modules/@fortawesome/fontawesome-free/css/all.min.css node_modules/vite-plugin-singlefile/dist/index.js node_modules/vite-plugin-pwa/dist/index.js node_modules/pinia/dist/pinia.mjs node_modules/@resvg/resvg-js/index.js 2>&1 | head -20
```
Expected: 列出 5 个文件路径，无 `No such file`。

- [ ] **Step 5: Commit**

```bash
cd "d:/repos/相册浏览器-pwa" && git add package.json package-lock.json && git commit -m "chore: 初始化 package.json 与依赖

- vue3/vite5/pinia 双 build 骨架依赖
- font-awesome/spark-md5/exif-js 内化资源
- @resvg/resvg-js 用于生成 PWA 图标"
```

---

## Task 2: 双 Vite 配置

**Files:**
- Create: `vite.config.js`
- Create: `vite.config.pwa.js`

- [ ] **Step 1: 写单 HTML build 配置**

Create `vite.config.js`:
```js
import { defineConfig } from 'vite';
import vue from '@vitejs/plugin-vue';
import { viteSingleFile } from 'vite-plugin-singlefile';

// 单 HTML 构建:所有 JS/CSS/字体(含 font-awesome woff2)base64 内联进单个 dist/index.html。
// 可 file:// 双击离线运行。base:'./' 用相对路径,适配本地文件与子路径部署。
export default defineConfig({
  plugins: [vue(), viteSingleFile()],
  base: './',
});
```

- [ ] **Step 2: 写 PWA build 配置**

Create `vite.config.pwa.js`:
```js
import { defineConfig } from 'vite';
import vue from '@vitejs/plugin-vue';
import { VitePWA } from 'vite-plugin-pwa';

// PWA 构建:独立外链 SW + manifest(无法内联进单 HTML,故与 vite.config.js 分开)。
// 两套 config 而非参数化,避免 if/else 污染主配置。
export default defineConfig({
  plugins: [
    vue(),
    VitePWA({
      registerType: 'autoUpdate',
      manifest: {
        name: '相册浏览器',
        short_name: '相册',
        description: '纯本地处理的相册浏览器:文件树、瀑布流、EXIF、文件整理。',
        theme_color: '#2d3e50',
        background_color: '#2d3e50',
        display: 'standalone',
        start_url: './',
        icons: [
          { src: 'icon-192.png', sizes: '192x192', type: 'image/png', purpose: 'any maskable' },
          { src: 'icon-512.png', sizes: '512x512', type: 'image/png', purpose: 'any maskable' },
        ],
      },
      workbox: {
        // 预缓存清单:补 woff2(源模板漏了字体,严格离线需要)
        globPatterns: ['**/*.{js,css,html,svg,png,json,wasm,woff2}'],
      },
    }),
  ],
  base: './',
});
```

- [ ] **Step 3: Commit**

```bash
cd "d:/repos/相册浏览器-pwa" && git add vite.config.js vite.config.pwa.js && git commit -m "chore: 双 Vite 配置(单HTML singlefile + PWA vite-plugin-pwa)"
```

---

## Task 3: index.html 入口

**Files:**
- Create: `index.html`

- [ ] **Step 1: 写入口 HTML**

Create `index.html`:
```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <!-- favicon:inline SVG data URI,不外链文件,保证单 HTML build 不破功 -->
  <link rel="icon" href="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24'%3E%3Crect width='24' height='24' rx='4' fill='%232d3e50'/%3E%3Cpath d='M3 7h6l2 2h10v10H3z' fill='%23fff'/%3E%3C/svg%3E">
  <title>相册浏览器</title>
</head>
<body>
  <div id="app"></div>
  <script type="module" src="/src/main.js"></script>
</body>
</html>
```

- [ ] **Step 2: Commit**

```bash
cd "d:/repos/相册浏览器-pwa" && git add index.html && git commit -m "feat: index.html 入口(inline SVG favicon + #app 挂载点)"
```

---

## Task 4: 搬源码 11 个 CSS 到 src/styles/

**Files:**
- Create: `src/styles/main.css`、`variables.css`、`base.css`、`layout.css`、`sidebar.css`、`components.css`、`gallery.css`、`modal.css`、`properties.css`、`theme-selector.css`、`context-menu.css`、`confirm-dialog.css`

源在 `D:\repos\相册浏览器\css\`。

- [ ] **Step 1: 创建目标目录并复制**

Run:
```bash
cd "d:/repos/相册浏览器-pwa" && mkdir -p src/styles && cp "d:/repos/相册浏览器/css/"*.css src/styles/ && ls src/styles/
```
Expected: 列出 12 个 `.css` 文件（main + 11 个模块）。

若中文路径在 git bash 报错，改用 node 复制：
```bash
cd "d:/repos/相册浏览器-pwa" && node -e "const fs=require('fs'),p=require('path');const s='d:/repos/相册浏览器/css',d='src/styles';fs.mkdirSync(d,{recursive:true});for(const f of fs.readdirSync(s).filter(f=>f.endsWith('.css')))fs.copyFileSync(p.join(s,f),p.join(d,f));console.log(fs.readdirSync(d).join('\\n'))"
```

- [ ] **Step 2: 核对 main.css 的 @import 路径**

Run:
```bash
cd "d:/repos/相册浏览器-pwa" && head -20 src/styles/main.css
```
Expected: 看到 `@import './variables.css'` 等相对路径（同目录引用，搬迁后仍有效）。若 main.css 用的是裸文件名 `@import 'variables.css'`，Vite 同样能解析，无需改。

- [ ] **Step 3: 扫描 CSS 内是否有外链资源引用**

Run:
```bash
cd "d:/repos/相册浏览器-pwa" && grep -nE "url\(" src/styles/*.css | grep -vE "data:|^\s*/\*" | head -20
```
Expected: 无输出，或仅是注释。源码 CSS 不引用外部图片/字体（图标走 font-awesome class，图片走运行时 blob），故应无 `url(./xxx)` 类引用。若有遗漏的相对资源，记下并在迁移日志标注。

- [ ] **Step 4: Commit**

```bash
cd "d:/repos/相册浏览器-pwa" && git add src/styles/ && git commit -m "feat: 搬入源码 11 个 CSS(设计系统 + 主题变量)"
```

---

## Task 5: main.js + App.vue 空壳 + Pinia

**Files:**
- Create: `src/main.js`
- Create: `src/App.vue`

- [ ] **Step 1: 写 main.js**

Create `src/main.js`:
```js
import { createApp } from 'vue';
import { createPinia } from 'pinia';
import App from './App.vue';

// 全局样式:搬自源码 css/(60+ 设计变量 + 3 套主题)
import './styles/main.css';

// font-awesome 内化:npm 包 ES import,Vite 自动接管字体(woff2)资源。
// 单 HTML build → base64 内联;PWA build → 独立哈希文件 + SW 预缓存。
import '@fortawesome/fontawesome-free/css/all.min.css';

const app = createApp(App);
app.use(createPinia());
app.mount('#app');
```

- [ ] **Step 2: 写 App.vue 骨架空壳**

Create `src/App.vue`:
```vue
<script setup>
// 阶段 0 骨架空壳:仅证明三件事——Vue 挂载成功、font-awesome 字体内化、设计变量生效。
// 阶段 1 起将替换为启动页/主界面真实结构。
</script>

<template>
  <div class="startup-placeholder">
    <i class="fas fa-images"></i>
    <h1>相册浏览器</h1>
    <p>骨架就绪 · 等待阶段 1 起接入功能</p>
  </div>
</template>

<style scoped>
.startup-placeholder {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 12px;
  background: var(--bg-secondary, #f5f7fa);
  color: var(--text-primary, #333);
}
.startup-placeholder i {
  font-size: 80px;
  color: var(--color-primary, #3498db);
}
.startup-placeholder h1 {
  font-size: 36px;
  margin: 0;
}
.startup-placeholder p {
  color: var(--text-secondary, #666);
}
</style>
```

- [ ] **Step 3: 验证单 HTML build 能通过**

Run:
```bash
cd "d:/repos/相册浏览器-pwa" && npm run build 2>&1 | tail -20
```
Expected: 构建成功，输出 `dist/index.html`，无报错。

- [ ] **Step 4: Commit**

```bash
cd "d:/repos/相册浏览器-pwa" && git add src/main.js src/App.vue && git commit -m "feat: main.js 入口 + App.vue 骨架空壳(Pinia + 全局 CSS + font-awesome)"
```

---

## Task 6: 生成 PWA 图标（192/512 PNG）

**Files:**
- Create: `public/icon.svg`（从源码 `D:\repos\相册浏览器\dist\icon.svg` 搬来）
- Create: `scripts/gen-icons.mjs`
- Create: `public/icon-192.png`、`public/icon-512.png`（脚本生成）

- [ ] **Step 1: 把源码 icon.svg 搬到 public/**

Run:
```bash
cd "d:/repos/相册浏览器-pwa" && mkdir -p public && cp "d:/repos/相册浏览器/dist/icon.svg" public/icon.svg && head -c 200 public/icon.svg
```
Expected: 看到 `<svg ...` 开头。若源文件不存在（路径变了），手写一个占位 svg：见下方 fallback。

Fallback（仅当上一步报 `No such file`）——Create `public/icon.svg`:
```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512">
  <rect width="512" height="512" rx="80" fill="#2d3e50"/>
  <path d="M96 176h120l40 40h160v160H96z" fill="#fff"/>
  <rect x="96" y="216" width="320" height="160" fill="#3498db" opacity="0.3"/>
</svg>
```

- [ ] **Step 2: 写图标生成脚本**

Create `scripts/gen-icons.mjs`:
```js
// 把 public/icon.svg 渲染成 PWA 需要的 192/512 PNG(maskable 占位)。
// 用 @resvg/resvg-js(纯 Rust/WASM,Windows 友好,无系统依赖)。
// 跑法:npm run icons   换 svg 后重跑即可。
import { Resvg } from '@resvg/resvg-js';
import { readFileSync, writeFileSync } from 'node:fs';
import { resolve, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';

const __dirname = dirname(fileURLToPath(import.meta.url));
const root = resolve(__dirname, '..');
const svg = readFileSync(resolve(root, 'public/icon.svg'), 'utf-8');

for (const [name, size] of [['icon-192.png', 192], ['icon-512.png', 512]]) {
  const resvg = new Resvg(svg, {
    fitTo: { mode: 'width', value: size },
    background: '#2d3e50',
  });
  writeFileSync(resolve(root, 'public', name), resvg.render().asPng());
  console.log(`✓ generated public/${name} (${size}×${size})`);
}
```

- [ ] **Step 3: 跑脚本生成 PNG**

Run:
```bash
cd "d:/repos/相册浏览器-pwa" && npm run icons
```
Expected: 两行 `✓ generated public/icon-192.png (192×192)` / `✓ generated public/icon-512.png (512×512)`。

- [ ] **Step 4: 验证 PNG 尺寸正确**

Run:
```bash
cd "d:/repos/相册浏览器-pwa" && node -e "const r=require('fs').readFileSync;for(const f of ['public/icon-192.png','public/icon-512.png']){const b=r(f);const w=b.readUInt32BE(16),h=b.readUInt32BE(20);console.log(f,w+'x'+h)}"
```
Expected:
```
public/icon-192.png 192x192
public/icon-512.png 512x512
```

- [ ] **Step 5: Commit**

```bash
cd "d:/repos/相册浏览器-pwa" && git add public/ scripts/ && git commit -m "feat: PWA 图标(icon.svg + 脚本生成 192/512 PNG 占位)"
```

---

## Task 7: 双 build 验收 + 机械核对

**Files:** 无新增，验证产物。

- [ ] **Step 1: 单 HTML build**

Run:
```bash
cd "d:/repos/相册浏览器-pwa" && rm -rf dist && npm run build 2>&1 | tail -15 && echo "=== dist 内容 ===" && ls -la dist/
```
Expected:
- 构建成功。
- `dist/` 下**只有 `index.html`**（单文件产物）。

- [ ] **Step 2: 核对单 HTML 内化了字体（base64）**

Run:
```bash
cd "d:/repos/相册浏览器-pwa" && grep -c "data:font/woff2" dist/index.html && du -h dist/index.html
```
Expected:
- 第一行输出 `≥ 1`（font-awesome 的 woff2 已 base64 内联进 CSS）。
- 文件体积应明显偏大（含字体，通常几百 KB ~ 1MB+）。

- [ ] **Step 3: PWA build**

Run:
```bash
cd "d:/repos/相册浏览器-pwa" && rm -rf dist && npm run build:pwa 2>&1 | tail -15 && echo "=== dist 内容 ===" && ls dist/
```
Expected:
- 构建成功。
- `dist/` 下应含：`index.html`、`manifest.webmanifest`、`sw.js`、`registerSW.js`、`icon-192.png`、`icon-512.png`、`assets/`、`workbox-*.js`。

- [ ] **Step 4: 核对 manifest 内容**

Run:
```bash
cd "d:/repos/相册浏览器-pwa" && cat dist/manifest.webmanifest
```
Expected: JSON 含 `"name":"相册浏览器"`、`"short_name":"相册"`、`"theme_color":"#2d3e50"`、两个 icon 引用。

- [ ] **Step 5: 核对 SW 预缓存含字体**

Run:
```bash
cd "d:/repos/相册浏览器-pwa" && grep -oE "fa-[a-z]+-[0-9]+-[A-Za-z0-9_]+\.woff2" dist/sw.js | head -5
```
Expected: 至少列出 `fa-solid-900-*.woff2` 等（说明字体进预缓存清单）。

- [ ] **Step 6: 清理本次验收产生的 dist（不入库）**

Run:
```bash
cd "d:/repos/相册浏览器-pwa" && rm -rf dist && git status --short
```
Expected: 工作区干净（dist 已在 .gitignore）。

> 验收产物无需 commit（dist 不入库）。本 task 是核对步骤，无代码改动则跳过 commit。

---

## Task 8: 交付用户 dev 验收

**Files:** 无。这是交接步骤。

- [ ] **Step 1: 汇总阶段 0 完成情况并交给用户 dev 验收**

向用户报告（中文）：
1. 阶段 0 已完成的 7 个 task 清单 + 每步验收结果。
2. 双 build 机械核对结果（单 HTML 含 base64 字体 / PWA manifest+SW 齐全）。
3. 请用户执行 `npm run dev`，在浏览器核对：
   - 页面显示「相册浏览器」标题 + 蓝色 `fa-images` 图标（证明 Vue 挂载 + font-awesome 内化）。
   - 标题颜色用到设计变量（蓝色，证明 `variables.css` 生效）。
   - 浏览器控制台**无报错**。
4. 等用户反馈「ok」后，再进入阶段 1（配置基础：config / file-types / theme store / browser-compat / utils）。

- [ ] **Step 2: 用户确认后，commit 一条阶段 0 收尾标记**

```bash
cd "d:/repos/相册浏览器-pwa" && git commit --allow-empty -m "chore: 阶段 0(工程骨架)完成,待用户 dev 验收"
```
（空 commit 仅作里程碑标记；若用户验收有问题，先修再标记。）

---

## Self-Review（plan 写完后自查，已执行）

1. **Spec 覆盖**：阶段 0 的所有产出（package.json / 双 config / index.html / main.js / App.vue / 搬 CSS / font-awesome / Pinia / PWA 图标 / 双 build）在 spec 第 5 节工程结构 + 第 8 节阶段 0 行均有对应。✓
2. **占位扫描**：无 TBD/TODO；所有代码块完整；图标生成有 fallback。✓
3. **类型/命名一致**：scripts/icons、build:pwa 命名跨 task 一致；icon-192/512 文件名与 manifest 引用一致。✓
4. **测试策略一致**：阶段 0 不写单测，与 spec 第 7.4 决策一致，已在 plan 开头声明。✓
