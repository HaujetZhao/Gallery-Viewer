# T11+T13 · useOverlay + 可访问性(合并批次)

> **For agentic workers:** REQUIRED SUB-SKILL: 用 `superpowers:executing-plans` 或 `subagent-driven-development` 按步执行。步骤用 `- [ ]` 跟踪。
> **父规划:** [2026-07-29-技术债治理-总规划.md](2026-07-29-技术债治理-总规划.md)
> ⚠️ **基于 master(含 T10)**:开工前确认 `debt/T10` 已合并 master。
> **合并批次**:T11(useOverlay 收口浮层 dismiss)+ T13(可访问性基础)。UI 改动,**主观验收为主**。

**Goal:**
- **T11**:抽 `useOverlay` composable 收口浮层 dismiss(ESC/点外/滚动)+ 焦点陷阱/归还,替换 RootSwitcher/ContextMenu/SettingsPanel/ConfirmDialog 4 份重复 `document.addEventListener`。
- **T13**:可访问性基础——PhotoCard 可键盘 focus/打开、SidebarTreeItem `role=treeitem`+`aria-expanded`、浮层 `role=dialog`+`aria-modal` + 焦点陷阱(useOverlay 提供)。

**Why:** 审查根因 3——浮层 dismiss 逻辑 4 份重复(每份 `onMounted` 自己 `addEventListener`)+ a11y 接近零(0 aria、0 role、卡片是 `<div>+@click` 不可 focus)。

**Tech Stack:** Vue 3 composable

---

# Part T11:useOverlay composable + 替换 4 浮层

## 一、新建 `src/composables/useOverlay.js`

- [ ] **Step T11.1:创建 useOverlay**

```js
// 浮层 dismiss 收口(ESC / 点外 / 滚动)+ 焦点陷阱/归还(T13 a11y)。
// 替换 RootSwitcher/ContextMenu/SettingsPanel/ConfirmDialog 各自的 document.addEventListener 重复。
import { nextTick, onBeforeUnmount, onMounted, watch } from 'vue';

// @param isVisible    () => boolean       浮层是否打开
// @param overlayEl    Ref<HTMLElement>    浮层根元素(点外检测 + 焦点陷阱范围)
// @param onClose      () => void          关闭回调
// @param outsideClick bool=false          点外关闭(ContextMenu/RootSwitcher 用)
// @param closeOnScroll bool=false         滚动关闭(ContextMenu 用)
// @param trapFocus    bool=false          焦点陷阱 + 归还(dialog 用)
export function useOverlay({ isVisible, overlayEl, onClose, outsideClick = false, closeOnScroll = false, trapFocus = false }) {
  let lastFocused = null;

  function close() { onClose?.(); }

  function onKeydown(e) {
    if (!isVisible())
      return;
    if (e.key === 'Escape') {
      e.stopPropagation();
      close();
      return;
    }
    if (trapFocus && e.key === 'Tab')
      trapTab(e);
  }

  function trapTab(e) {
    const el = overlayEl?.value;
    if (!el)
      return;
    const focusable = el.querySelectorAll('button:not([disabled]), [href], input:not([disabled]), select:not([disabled]), textarea:not([disabled]), [tabindex]:not([tabindex="-1"])');
    if (!focusable.length)
      return;
    const first = focusable[0];
    const last = focusable[focusable.length - 1];
    if (e.shiftKey && document.activeElement === first) {
      e.preventDefault();
      last.focus();
    }
    else if (!e.shiftKey && document.activeElement === last) {
      e.preventDefault();
      first.focus();
    }
  }

  function onDocClick(e) {
    if (!isVisible() || !outsideClick)
      return;
    const el = overlayEl?.value;
    if (el && !el.contains(e.target))
      close();
  }

  function onScroll() {
    if (isVisible() && closeOnScroll)
      close();
  }

  // 焦点:打开时记上次焦点 + 聚焦浮层;关闭时归还
  watch(
    () => isVisible(),
    (v) => {
      if (v) {
        if (trapFocus) {
          lastFocused = document.activeElement;
          nextTick(() => {
            const el = overlayEl?.value;
            if (!el)
              return;
            const first = el.querySelector('button, [href], input, [tabindex="-1"]');
            (first || el).focus?.();
          });
        }
      }
      else if (trapFocus && lastFocused) {
        lastFocused.focus?.();
        lastFocused = null;
      }
    },
  );

  onMounted(() => {
    document.addEventListener('keydown', onKeydown);
    if (outsideClick)
      document.addEventListener('click', onDocClick);
    if (closeOnScroll)
      window.addEventListener('scroll', onScroll, true);
  });
  onBeforeUnmount(() => {
    document.removeEventListener('keydown', onKeydown);
    document.removeEventListener('click', onDocClick);
    window.removeEventListener('scroll', onScroll, true);
  });
}
```

## 二、4 浮层用 useOverlay 替换各自 dismiss

- [ ] **Step T11.2:[RootSwitcher.vue](../../../src/components/RootSwitcher.vue)** —— 点外 + ESC

删 `onDocClick` / `onKeydown` / `onMounted` / `onBeforeUnmount`(L44-59)。给 dropdown 加 ref(L69 `<div v-if="open" ref="dropdownEl" ...>`)。script 加:
```js
import { ref } from 'vue';  // 已有
import { useOverlay } from '../composables/useOverlay';
const dropdownEl = ref(null);
useOverlay({
  isVisible: () => open.value,
  overlayEl: dropdownEl,
  onClose: close,
  outsideClick: true,
});
```
(点 toggle 按钮 `@click.stop` 已阻止冒泡,不会误触发点外关。)

- [ ] **Step T11.3:[ContextMenu.vue](../../../src/components/ContextMenu.vue)** —— 点外 + ESC + 滚动

删 `onDocClick` / `onKeydown` / `onScroll` / `onMounted` / `onBeforeUnmount`(L33-54)。script 加(menuEl 已有 ref):
```js
import { useOverlay } from '../composables/useOverlay';
useOverlay({
  isVisible: () => menu.visible,
  overlayEl: menuEl,
  onClose: () => menu.hide(),
  outsideClick: true,
  closeOnScroll: true,
});
```

- [ ] **Step T11.4:[SettingsPanel.vue](../../../src/components/SettingsPanel.vue)** —— 只 ESC(拖拽面板不点外关)

删 `onKeydown` / `onMounted`(ESC 部分,L76-80;保留拖拽 onDragStart/Move/End)。script 加(panelEl 已有 ref):
```js
import { useOverlay } from '../composables/useOverlay';
useOverlay({
  isVisible: () => props.modelValue,
  overlayEl: panelEl,
  onClose: () => emit('update:modelValue', false),
  // outsideClick=false(拖拽面板不点外关)、closeOnScroll=false、trapFocus=false
});
```

- [ ] **Step T11.5:[ConfirmDialog.vue](../../../src/components/ConfirmDialog.vue)** —— ESC + 焦点陷阱

删 `onKeydown` / `onMounted` / `onBeforeUnmount`(L8-16)。保留 overlay `@click.self="confirm.cancel"`(点遮罩关,这是它自己的)。script 加(boxEl 已有 ref):
```js
import { useOverlay } from '../composables/useOverlay';
useOverlay({
  isVisible: () => confirm.visible,
  overlayEl: boxEl,
  onClose: () => confirm.cancel(),
  trapFocus: true,  // T13:焦点陷阱 + 归还
});
```

---

# Part T13:可访问性基础

## 三、PhotoCard 可键盘 focus + 打开

- [ ] **Step T13.1:[PhotoCard.vue](../../../src/components/PhotoCard.vue)**

卡片当前是 `<div ... @click>`。加 `tabindex="0"` + `role="button"` + 键盘 Enter/Space 打开(找卡片根 div,加属性 + `@keydown.enter`/`@keydown.space.prevent`):
```html
<div
  class="photo-card"
  tabindex="0"
  role="button"
  :aria-label="`查看 ${file.name}`"
  @click="..."
  @keydown.enter="...(同 click 的打开逻辑)"
  @keydown.space.prevent="...(同上)"
>
```
(把打开预览的逻辑提取成 `openPreview()`,click + keydown 共用。)

## 四、SidebarTreeItem role + aria-expanded

- [ ] **Step T13.2:[SidebarTreeItem.vue](../../../src/components/SidebarTreeItem.vue)**

树节点加 `role="treeitem"` + `aria-expanded`(绑 folder.expanded)。找节点根元素(li 或 div),加:
```html
<li role="treeitem" :aria-expanded="folder.expanded" ...>
```

## 五、浮层 role=dialog + aria-modal + tabindex(焦点陷阱用)

- [ ] **Step T13.3:ConfirmDialog/SettingsPanel/MediaModal 浮层加 role**

- [ConfirmDialog.vue](../../../src/components/ConfirmDialog.vue) boxEl(L36):`<div ref="boxEl" class="confirm-dialog" role="dialog" aria-modal="true" tabindex="-1">`
- [SettingsPanel.vue](../../../src/components/SettingsPanel.vue) panelEl(L174):`role="dialog" aria-modal="true" tabindex="-1"`
- [MediaModal.vue](../../../src/components/MediaModal.vue) modalEl:加 `role="dialog" aria-modal="true"`
(`tabindex="-1"` 让 useOverlay 的焦点陷阱能聚焦浮层本身。MediaModal 暂不接 useOverlay——它有独立手势系统,只加 role/aria。)

## 六、focus-visible 样式(键盘焦点可见)

- [ ] **Step T13.4:全局加 focus-visible 轮廓**

[base.css](../../../src/styles/base.css) 加:
```css
:focus-visible {
  outline: 2px solid var(--color-primary);
  outline-offset: 2px;
}
```

---

## 七、验收

- [ ] **Step V.1:grep 确认重复 addEventListener 减少**
```bash
grep -rn "document.addEventListener('keydown'" src/components/
```
Expected: 4 浮层的 keydown 监听清零(全收口到 useOverlay;ContextMenu 拖拽的 mousemove 不算)。

- [ ] **Step V.2:双 build + 测试 + Lint**
```bash
rm -rf dist && npm run build
rm -rf dist && npm run build:pwa
npm test && npm run lint
```

---

## 八、验收点

**客观断言:**
- useOverlay.js 新建;4 浮层各自 dismiss 替换为 useOverlay 调用;4 浮层 keydown 监听清零。
- PhotoCard tabindex=0+role=button;SidebarTreeItem role=treeitem+aria-expanded;3 浮层 role=dialog+aria-modal;:focus-visible 样式。

**主观体验(产品负责人,键盘上手——这是 a11y 核心):**
1. **Tab 导航**:Tab 能聚焦卡片(可见焦点轮廓)+ 卡片 Enter/Space 打开预览。
2. **浮层焦点陷阱**:打开确认框 → Tab 只在框内循环(不跑到背后)+ ESC 关 + 关后焦点归还触发它的元素。
3. **侧栏树**:aria-expanded 随展开/折叠变化(可用屏幕阅读器或 DOM 查验)。
4. **点外/ESC/滚动关闭**仍正常(RootSwitcher/ContextMenu 点外关、ContextMenu 滚动关、全部 ESC 关)——不回归。

---

## 九、变更技术报告(执行者完成后填写)

```
## 变更技术报告 — T11+T13

### 分支
- debt/T11T13-useOverlay-a11y,基于 debt/T10。计划「基于 master(含 T10)」为理想化表述;实际 debt 链均未合 master,按惯例基于链上最新 T10 续建,含 T10 全部代码。

### T11 · useOverlay 收口浮层 dismiss
- ✅ 新建 src/composables/useOverlay.js:可配置 outsideClick / closeOnScroll / trapFocus;onMounted 注册、onBeforeUnmount 注销 ESC/点外/滚动(capture)监听;trapFocus 模式 watch(isVisible) 记上次焦点→聚焦浮层首元素→关闭归还。
- ✅ RootSwitcher.vue:删 onDocClick/onKeydown/onMounted/onBeforeUnmount → useOverlay({ outsideClick:true });dropdown 补 ref="dropdownEl"(点外检测范围)。toggle 按钮 @click.stop 已阻止冒泡,不误触发点外关。
- ✅ ContextMenu.vue:删 onDocClick/onKeydown/onScroll + onMounted/onBeforeUnmount → useOverlay({ outsideClick:true, closeOnScroll:true });menuEl 复用,定位 watch 保留。
- ✅ SettingsPanel.vue:删 onKeydown + onMounted(ESC 部分)→ useOverlay({})(仅 ESC;outsideClick=false 拖拽面板不点外关);保留拖拽 onDragStart/Move/End + onBeforeUnmount 的 mousemove/mouseup 兜底注销。
- ✅ ConfirmDialog.vue:删 onKeydown/onMounted/onBeforeUnmount → useOverlay({ trapFocus:true })(ESC + 焦点陷阱/归还);overlay @click.self 点遮罩关保留(不走 useOverlay outsideClick);进场缩放 watch 与 useOverlay 焦点 watch 独立共存。

### T13 · 可访问性基础
- ✅ PhotoCard.vue:根 div 加 tabindex="0" + role="button" + :aria-label="查看 ${file.name}";提取 openPreview()(emit click),@click/@keydown.enter/@keydown.space.prevent 共用。
- ✅ SidebarTreeItem.vue:li 加 role="treeitem" + :aria-expanded="folder.expanded"。
- ✅ 3 浮层 role:ConfirmDialog boxEl / SettingsPanel panelEl 加 role="dialog" aria-modal="true" tabindex="-1";MediaModal modalEl 加 role="dialog" aria-modal="true"(独立手势系统,不接 useOverlay,只加 role/aria)。
- ✅ base.css:加 :focus-visible { outline: 2px solid var(--color-primary); outline-offset: 2px; }。

### grep 硬指标(已核实)
- src/components/ 下 addEventListener('keydown' → 0 处(4 浮层全部清零)。
- 全 src 仅剩 3 处,均合理保留:useOverlay.js:84(收口后唯一持有者)/ App.vue:65(App 全局快捷键)/ useModal.js:360(MediaModal 手势系统 window 级监听)。

### 验收基线
- lint ✅(0 error / 0 warning)  test ✅(15 files / 95 tests,4.76s)  build ✅(单 HTML dist/index.html 1.42MB,975ms)  build:pwa ✅(SW+manifest+woff2 预缓存 14 entries,878ms)

### 主观验收(产品负责人,待用户浏览器键盘上手——按约定不代做)
1. Tab 聚焦卡片(可见 :focus-visible 轮廓)+ Enter/Space 打开预览:待验收
2. 浮层焦点陷阱(打开确认框 → Tab 只在框内循环 + ESC 关 + 关后焦点归还触发元素):待验收
3. 侧栏树 aria-expanded 随展开/折叠变化:待验收
4. dismiss 不回归(RootSwitcher/ContextMenu 点外关、ContextMenu 滚动关、4 浮层 ESC 关):待验收

### 遗留 / 风险 / 偏离
- SidebarTreeItem aria-expanded 直接绑 folder.expanded,叶子节点(无 subFolders)也带。严格 ARIA 树模式应按 subFolders.length 条件输出。本轮按计划「基础可达性」保持简单;若屏幕阅读器体验不佳,改 :aria-expanded="folder.subFolders.length ? folder.expanded : undefined"。
- trapFocus 打开 ConfirmDialog 时聚焦框内首个 button(「下一步/确认」),符合常见对话框交互;若期望聚焦「取消」可调选择器。
- 完整 tree ARIA(ul role=tree/group)、ContextMenu role=menu 等**不在本轮范围**(计划明确「只做基础可达性,不加新交互」)。
```

---

## 十、执行者注意

1. **useOverlay 可配置**:outsideClick/closeOnScroll/trapFocus 按浮层需求(RootSwitcher/ContextMenu 点外;ContextMenu 滚动;ConfirmDialog 焦点陷阱;SettingsPanel 只 ESC)。
2. **ConfirmDialog 的 overlay @click.self 保留**(点遮罩关是它自己的,不走 useOverlay outsideClick)。
3. **SettingsPanel 拖拽逻辑保留**(onDragStart/Move/End),只删 ESC 部分。
4. **PhotoCard 打开逻辑提取成 openPreview()**,click + keydown 共用,别复制。
5. **a11y 是工程缺口(非功能)**——只做基础可达性(role/tabindex/aria/焦点陷阱),不加新交互。
6. UI 改动主观验收为主,别用 Playwright 测手感(违反约定);客观(grep/DOM role/aria)可查。
7. 双 build 分别 `rm -rf dist`。
