# 阶段 2：数据模型 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans. Steps use checkbox (`- [ ]`) syntax.

**Goal:** 迁移源码三个数据模型（SmartFile / TreeNode / SmartFolder）+ 建 fsStore（appState 容器）。里程碑：dev 里点按钮打开本地文件夹 → scan → 控制台打印出文件树（验证增量扫描算法迁移正确）。

**Architecture:**
- 三个 model 保持 **plain JS class，不 import Vue/Pinia**（纯逻辑，可单测、可复用）。
- `appState`（rootHandle/foldersData 等）进 Pinia `useFsStore`；通过**静态注入** `SmartFolder.appState = { get rootHandle(), get foldersData() }` 让 model 访问，避免 model 反向依赖 store。
- **TreeNode 从 DOM 操作类退化为纯数据节点**：删所有 DOM 方法，新增 `expanded/active/isEmpty` 字段替代 class；`syncChildren` diff 算法保留，只把 addChild/removeChild 换成数据操作。
- **domToFolderMap 废弃**（Vue 后用 `@click="select(folder)"` 传对象引用，不需要 DOM↔对象反向映射）。

**Tech Stack:** Pinia setup store、File System Access API（`showDirectoryPicker` 临时用于验收，阶段 4 filesystem 做好后替换）。

**关于测试：** scan 增量算法、syncChildren diff 是核心复杂逻辑，阶段 2 用「dev 打开真实文件夹扫描、控制台打印树」做端到端验证（node 跑不了，依赖 FS Access API）。纯函数（getActualType 魔数）在阶段 3 缩略图实际使用时自然验证。

**关于照搬：** SmartFile 全文 plan 已给（含修 bug）；SmartFolder 标注「照搬源码 + 改造点」，scan 算法贴完整；TreeNode 给退化版目标代码。

---

## File Structure

| 文件 | 职责 | 来源 |
|---|---|---|
| `src/models/SmartFile.js` | 文件模型（handle/file/blobUrl/md5/parent + rename/move/refresh/validate/getActualType/dispose） | model.SmartFile.js（照搬 + 修 refresh bug + move 去 updateCount） |
| `src/models/TreeNode.js` | 文件树数据节点（folder/children/expanded/active/isEmpty + addChild/removeChild/syncChildren/toggleExpanded/setActive/destroy）+ treeNodeRegistry | model.TreeNode.js（**退化为纯数据节点**） |
| `src/models/SmartFolder.js` | 文件夹模型（handle/files/subFolders/scanned/treeNode + scan 增量算法 + create/createVirtual）+ createAllMediaFolder 工厂 | model.SmartFolder.js（照搬 + 改造点） |
| `src/stores/fs.js` | Pinia：rootHandle/currentFolder/foldersData/allMediaFolder + openRoot + 注入 SmartFolder.appState + ALL_MEDIA 初始化 | state.js 的 appState + SmartFolder 模块加载副作用 |

---

## Task 1: models/SmartFile.js（照搬 + 修 bug）

**Files:**
- Create: `src/models/SmartFile.js`

- [ ] **Step 1: 写 SmartFile.js**

Create `src/models/SmartFile.js`（照搬源码，3 处改造：加 export、`refresh()` 删遮蔽 getter 的赋值、`move()` 删 updateCount 调用）:

```js
/**
 * SmartFile 类 - 表示一个媒体文件。搬自源码 js/model.SmartFile.js,零外部依赖。
 * blobUrl 构造时立即建;md5 是懒加载槽(外部算后赋值)。
 */
export class SmartFile {
  constructor({ handle, file, parent = null }) {
    this.handle = handle; // FileSystemFileHandle
    this.file = file; // File 对象
    this.parent = parent; // 父 SmartFolder 引用
    this.blobUrl = URL.createObjectURL(file);
    this.dom = null; // deprecated: gallery 卡片 DOM 反向引用,新代码不依赖
    this.md5 = null; // 懒加载,外部计算后赋值
  }

  _extractType(filename) {
    const parts = filename.split('.');
    if (parts.length < 2) return '';
    return parts.pop().toLowerCase();
  }

  get name() {
    return this.handle.name;
  }
  get size() {
    return this.file.size;
  }
  get lastModified() {
    return this.file.lastModified;
  }
  get type() {
    return this._extractType(this.name);
  }

  get path() {
    const parts = [this.name];
    let current = this.parent;
    while (current) {
      parts.unshift(current.name);
      current = current.parent;
    }
    return parts.join('/');
  }

  async rename(newName) {
    if (!this.handle || !this.parent) {
      throw new Error('无法重命名：缺少必要的句柄或父级引用');
    }
    try {
      await this.handle.move(newName);
      const newFile = await this.handle.getFile();
      this.file = newFile;
      if (this.blobUrl) URL.revokeObjectURL(this.blobUrl);
      this.blobUrl = URL.createObjectURL(newFile);
      this.md5 = null;
      return true;
    } catch (err) {
      console.error('重命名失败:', err);
      throw err;
    }
  }

  async move(targetFolder) {
    if (!this.parent || !this.parent.handle) {
      throw new Error('无法移动：缺少父级引用');
    }
    if (!targetFolder || !targetFolder.handle) {
      throw new Error('无法移动：目标文件夹无效');
    }
    try {
      const sourceFolder = this.parent;
      await this.handle.move(targetFolder.handle);
      sourceFolder.removeFile(this);
      this.parent = targetFolder;
      targetFolder.addFileAndSort(this);
      // 源码此处还有 sourceFolder.updateCount()/targetFolder.updateCount()
      // 退化后 count 是计算属性(folder.files.length),无需手动更新,已删
      return true;
    } catch (err) {
      console.error('移动失败:', err);
      throw err;
    }
  }

  async refresh() {
    try {
      const file = await this.handle.getFile();
      if (this.size !== file.size || this.lastModified !== file.lastModified) {
        if (this.blobUrl) URL.revokeObjectURL(this.blobUrl);
        this.file = file;
        // 源码原本还有 this.size = file.size / this.lastModified = file.lastModified,
        // 会遮蔽 getter,冗余且怪异,已删(getter 本就读 this.file)
        this.blobUrl = URL.createObjectURL(file);
        this.md5 = null;
      }
      return true;
    } catch (err) {
      console.error('刷新文件失败:', err);
      throw err;
    }
  }

  dispose() {
    if (this.blobUrl) {
      URL.revokeObjectURL(this.blobUrl);
      this.blobUrl = null;
    }
  }

  async validate() {
    if (!this.handle) return false;
    try {
      await this.handle.getFile();
      return true;
    } catch (err) {
      if (err.name === 'NotFoundError') return false;
      console.warn(`文件 ${this.name} 验证失败:`, err);
      return false;
    }
  }

  // 文件头魔数识别实际类型。位运算逐字符照搬源码。
  async getActualType() {
    try {
      const file = await this.handle.getFile();
      const buffer = await file.slice(0, 12).arrayBuffer();
      const bytes = new Uint8Array(buffer);

      if (bytes.length >= 8 && bytes[0] === 0x89 && bytes[1] === 0x50 && bytes[2] === 0x4e && bytes[3] === 0x47)
        return 'png';
      if (bytes.length >= 3 && bytes[0] === 0xff && bytes[1] === 0xd8 && bytes[2] === 0xff) return 'jpg';
      if (bytes.length >= 4 && bytes[0] === 0x47 && bytes[1] === 0x49 && bytes[2] === 0x46 && bytes[3] === 0x38)
        return 'gif';
      if (
        bytes.length >= 12 &&
        bytes[0] === 0x52 &&
        bytes[1] === 0x49 &&
        bytes[2] === 0x46 &&
        bytes[3] === 0x46 &&
        bytes[8] === 0x57 &&
        bytes[9] === 0x45 &&
        bytes[10] === 0x42 &&
        bytes[11] === 0x50
      )
        return 'webp';
      if (bytes.length >= 2 && bytes[0] === 0x42 && bytes[1] === 0x4d) return 'bmp';

      if (this.type === 'svg') {
        const text = await file.slice(0, 1000).text();
        if (text.includes('<svg') || text.includes('<?xml')) return 'svg';
      }
      return this.type;
    } catch (err) {
      console.warn(`获取文件 ${this.name} 实际类型失败:`, err);
      return this.type;
    }
  }
}
```

- [ ] **Step 2: Commit**

```bash
cd "d:/repos/相册浏览器-pwa" && git add src/models/SmartFile.js && git commit -m "feat(阶段2): SmartFile 模型(照搬 + 修 refresh 遮蔽 getter bug)"
```

---

## Task 2: models/TreeNode.js（退化为纯数据节点）

**Files:**
- Create: `src/models/TreeNode.js`

- [ ] **Step 1: 写退化版 TreeNode.js**

Create `src/models/TreeNode.js`（删所有 DOM 方法，新增 expanded/active/isEmpty 字段，保留 syncChildren diff 算法）:

```js
/**
 * TreeNode - 文件树数据节点。搬自源码 js/model.TreeNode.js 并退化为纯数据节点。
 * 源码原本是 DOM 操作类(create*/setActive class 等),Vue 后 DOM 渲染交给 Sidebar.vue 的 v-for,
 * 本类只保留树数据结构 + 展开/激活/空状态 + syncChildren diff 算法(用于增量扫描后同步状态)。
 */
import { windowsCompareStrings } from '../utils/format';

// 模块级注册表:仅做内存管理(_unregister),不再管 activeNode(激活态单一来源交 store.currentFolder)
export const treeNodeRegistry = {
  nodes: new Set(),
  register(node) {
    this.nodes.add(node);
  },
  unregister(node) {
    this.nodes.delete(node);
  },
};

export class TreeNode {
  constructor(folder) {
    this.folder = folder;
    this.children = [];
    this.expanded = true;
    this.active = false;
    this.isEmpty = true;
    treeNodeRegistry.register(this);
  }

  get count() {
    return this.folder.files.length;
  }

  refreshState() {
    // scan 之后调用,重算空状态
    this.isEmpty = this.folder.files.length === 0 && this.folder.subFolders.length === 0;
  }

  setActive() {
    this.active = true;
  }
  setInactive() {
    this.active = false;
  }

  toggleExpanded() {
    this.expanded = !this.expanded;
  }

  // 按名称排序插入到 children 数组(替代源码的 insertBefore DOM 操作)
  addChild(childNode) {
    if (this.children.includes(childNode)) return;
    this.children.push(childNode);
    this.children.sort((a, b) => windowsCompareStrings(a.folder.name, b.folder.name));
  }

  removeChild(childNode) {
    const i = this.children.indexOf(childNode);
    if (i > -1) this.children.splice(i, 1);
  }

  // 核心 diff 算法:增量扫描后同步子节点,保留已存在节点对象(展开/激活态不丢)。
  // 算法逻辑一字不改照搬源码,仅 addChild/removeChild 已改为数据操作。
  async syncChildren(subFolders) {
    // 1. 找出需要移除的节点
    const newFolderNames = new Set(subFolders.map((f) => f.name));
    const currentChildren = [...this.children]; // 复制,removeChild 会改 this.children

    for (const childNode of currentChildren) {
      if (!newFolderNames.has(childNode.folder.name)) {
        this.removeChild(childNode);
      }
    }

    // 2. 找出需要添加的节点
    const currentFolderNames = new Set(this.children.map((c) => c.folder.name));

    for (const subFolder of subFolders) {
      if (!currentFolderNames.has(subFolder.name)) {
        if (!subFolder.treeNode) {
          console.error('Subfolder missing treeNode', subFolder);
          continue;
        }
        if (!subFolder.scanned) {
          await subFolder.scan();
        }
        this.addChild(subFolder.treeNode);
      }
    }
  }

  destroy() {
    treeNodeRegistry.unregister(this);
    this.children = [];
  }
}
```

- [ ] **Step 2: Commit**

```bash
cd "d:/repos/相册浏览器-pwa" && git add src/models/TreeNode.js && git commit -m "feat(阶段2): TreeNode 退化为纯数据节点(删 DOM 方法,保留 syncChildren diff)"
```

---

## Task 3: models/SmartFolder.js（scan 照搬 + appState 注入）

**Files:**
- Create: `src/models/SmartFolder.js`

- [ ] **Step 1: 读源码并迁移**

读 `D:\repos\相册浏览器\js\model.SmartFolder.js` 全文，按以下改造点迁移到 `src/models/SmartFolder.js`：

**改造点清单：**

1. **加 export**：`export class SmartFolder`、`export function createAllMediaFolder`。
2. **import 依赖**：
   ```js
   import { SmartFile } from './SmartFile.js';
   import { TreeNode } from './TreeNode.js';
   import { FileTypes } from '../config/file-types.js';
   import { windowsCompareStrings } from '../utils/format.js';
   ```
3. **appState 静态注入**：类里加 `static appState = null;`（由 fsStore 在初始化时注入 `{ get rootHandle(), get foldersData() }`）。源码里所有 `appState.xxx` 改成 `SmartFolder.appState.xxx`。
4. **path getter 简化**：源码两个分支返回相同字符串，简化为单一 parent 链拼接：
   ```js
   get path() {
     if (this.isVirtual && this.virtualConfig?.customPath) return this.virtualConfig.customPath;
     const parts = [this.name];
     let current = this.parent;
     while (current) { parts.unshift(current.name); current = current.parent; }
     return parts.join('/');
   }
   ```
5. **scan 算法原样保留**（见 Step 2 完整代码），唯一改动：`folderObj.removeDOMNodes()` → `folderObj.treeNode?.destroy()`。
6. **修 getAllFiles bug**：`for (const child of this.children)` → `this.subFolders`。
7. **删 deprecated**：`clearSubtree` / `rescanSubtree`（连同对 syncTreeStructure 的依赖）。
8. **删 UI 代理方法**：`updateCount` / `updateIconState` / `toggleExpanded` / `setActive` / `removeDOMNodes` / `initUI`（这些是转发给 treeNode 改 DOM 的，退化后 Vue 组件直接读 treeNode 数据字段，folder 不需要）。⚠ 检查是否有其他保留方法调用了它们——若有，一并清理。
9. **createAllMediaFolder 工厂导出**，**去掉模块加载副作用**：删除文件末尾的 `const ALL_MEDIA_FOLDER = createAllMediaFolder(); appState.foldersData.set('ALL_MEDIA', ALL_MEDIA_FOLDER);`（挪到 fsStore 初始化）。
10. **保留的方法**：构造、`static create` / `static createVirtual`、`addFile` / `addFileAndSort` / `removeFile` / `findFile` / `getFileCount` / `getAllFiles`(已修) / `dispose`、`validate` / `findValidAncestor`、`delete`。

- [ ] **Step 2: scan 算法完整代码（照搬，仅 removeDOMNodes 一行改）**

`scan()` 方法用以下代码（其余方法照源码搬）：

```js
async scan() {
  if (!this.handle) throw new Error('scan 需要有效的 handle');
  const dirHandle = this.handle;

  // ① 建立现有 files/subFolders 的 name→obj 映射(用于差集)
  const existingFilesMap = new Map(this.files.map((f) => [f.name, f]));
  const existingFoldersMap = new Map(this.subFolders.map((f) => [f.name, f]));

  const filesToKeep = [];
  const foldersToKeep = [];
  const newFiles = [];
  const newSubFolders = [];

  // ② 单次遍历目录
  for await (const entry of dirHandle.values()) {
    if (entry.kind === 'file') {
      const ext = entry.name.split('.').pop().toLowerCase();
      if (!FileTypes.allMedia.includes(ext)) continue; // 仅媒体

      const existingFile = existingFilesMap.get(entry.name);
      if (existingFile) {
        try {
          const file = await entry.getFile();
          if (existingFile.size !== file.size || existingFile.lastModified !== file.lastModified) {
            await existingFile.refresh(); // 复用对象,只刷新
          }
          filesToKeep.push(existingFile);
          existingFilesMap.delete(entry.name);
        } catch (e) {
          console.log(`文件 ${entry.name} 的 handle 已失效，将被移除`);
        }
      } else {
        try {
          const file = await entry.getFile();
          const fileObj = new SmartFile({ handle: entry, file, parent: this });
          filesToKeep.push(fileObj);
          newFiles.push(fileObj);
        } catch (e) {
          console.warn('无法读取文件:', entry.name, e);
        }
      }
    } else if (entry.kind === 'directory') {
      if (entry.name.startsWith('.')) continue; // 跳过隐藏目录

      const existingFolder = existingFoldersMap.get(entry.name);
      if (existingFolder) {
        foldersToKeep.push(existingFolder);
        existingFoldersMap.delete(entry.name);
      } else {
        const subFolderData = new SmartFolder({ handle: entry, parent: this });
        const subPath = this.path + '/' + entry.name;
        SmartFolder.appState.foldersData.set(subPath, subFolderData); // 全局注册
        foldersToKeep.push(subFolderData);
        newSubFolders.push(subFolderData);
      }
    }
  }

  // ③ 清理"已被删除"的:留在 Map 里没被 delete 的 = 目录里已不存在
  for (const fileObj of existingFilesMap.values()) {
    fileObj.dispose();
  }
  for (const folderObj of existingFoldersMap.values()) {
    const deletedPath = folderObj.path;
    SmartFolder.appState.foldersData.delete(deletedPath);
    folderObj.treeNode?.destroy(); // 源码是 removeDOMNodes(),退化后改为数据清理
  }

  // ④ 排序(Windows 风格)
  filesToKeep.sort((a, b) => windowsCompareStrings(a.name, b.name));
  foldersToKeep.sort((a, b) => windowsCompareStrings(a.name, b.name));

  this.files = filesToKeep;
  this.subFolders = foldersToKeep;
  this.scanned = true;
  this.treeNode?.refreshState();

  return {
    folder: this,
    newFiles,
    newSubFolders,
    removedFileCount: existingFilesMap.size,
    removedFolderCount: existingFoldersMap.size,
  };
}
```

> 注：源码 scan 有大量 `performance.now()` 调试日志，迁移时删除（噪音）。

- [ ] **Step 3: 验证 import 链无误**

Run:
```bash
cd "d:/repos/相册浏览器-pwa" && node --input-type=module -e "
import('./src/models/SmartFolder.js').then(m => {
  console.log('SmartFolder:', typeof m.SmartFolder, '| createAllMediaFolder:', typeof m.createAllMediaFolder);
  const v = m.createAllMediaFolder();
  console.log('ALL_MEDIA virtual:', v.isVirtual, v.name, '| files:', v.files.length, '| scanned:', v.scanned);
}).catch(e => { console.error('ERR', e.message); process.exit(1); })
"
```
Expected: 打印 `SmartFolder: function | createAllMediaFolder: function` 和 `ALL_MEDIA virtual: true ALL_MEDIA | files: 0 | scanned: false`（确认三个 model 互引无误、虚拟文件夹可建）。

- [ ] **Step 4: Commit**

```bash
cd "d:/repos/相册浏览器-pwa" && git add src/models/SmartFolder.js && git commit -m "feat(阶段2): SmartFolder 模型(scan 增量算法照搬 + appState 静态注入 + 修 getAllFiles bug)"
```

---

## Task 4: stores/fs.js（Pinia + openRoot + 注入）

**Files:**
- Create: `src/stores/fs.js`

- [ ] **Step 1: 写 fs store**

Create `src/stores/fs.js`:
```js
// 文件系统 store。搬自源码 js/state.js 的 appState,改为 Pinia setup store。
// 持有 rootHandle/currentFolder/foldersData/allMediaFolder;通过静态注入让 SmartFolder 访问(保持 model 纯逻辑)。
import { defineStore } from 'pinia';
import { ref } from 'vue';
import { SmartFolder, createAllMediaFolder } from '../models/SmartFolder';

export const useFsStore = defineStore('fs', () => {
  const rootHandle = ref(null); // FileSystemDirectoryHandle
  const currentFolder = ref(null); // SmartFolder
  const foldersData = ref(new Map()); // path -> SmartFolder(去重缓存)
  const allMediaFolder = ref(null); // ALL_MEDIA 虚拟文件夹

  // 静态注入:SmartFolder 通过 SmartFolder.appState.foldersData/rootHandle 访问 store,
  // 不直接 import pinia,保持 model 纯逻辑。
  SmartFolder.appState = {
    get rootHandle() {
      return rootHandle.value;
    },
    get foldersData() {
      return foldersData.value;
    },
  };

  // 初始化 ALL_MEDIA 虚拟文件夹(原 model.SmartFolder.js 模块加载副作用挪到这里)
  allMediaFolder.value = createAllMediaFolder();
  foldersData.value.set('ALL_MEDIA', allMediaFolder.value);

  // 临时:打开根目录并扫描。阶段 4 filesystem service 做好后替换为完整实现(含权限/recovery)。
  async function openRoot() {
    const handle = await window.showDirectoryPicker({
      mode: 'readwrite',
      id: 'photo-viewer-start',
      startIn: 'pictures',
    });
    rootHandle.value = handle;
    const result = await SmartFolder.create({ handle });
    currentFolder.value = result.folder;
    foldersData.value.set(result.folder.path, result.folder);
    return result;
  }

  return { rootHandle, currentFolder, foldersData, allMediaFolder, openRoot };
});
```

- [ ] **Step 2: Commit**

```bash
cd "d:/repos/相册浏览器-pwa" && git add src/stores/fs.js && git commit -m "feat(阶段2): fs Pinia store(appState 容器 + openRoot + 静态注入 SmartFolder)"
```

---

## Task 5: App.vue 临时 scan 按钮 + 验收

**Files:**
- Modify: `src/App.vue`

- [ ] **Step 1: App.vue 加临时 scan 按钮**

在 `src/App.vue` 的 `<script setup>` 顶部加 fsStore 引用与 scan 打印函数，并在 template 主题切换器下方加按钮。修改如下：

`<script setup>` 增加：
```js
import { useFsStore } from './stores/fs';

const fsStore = useFsStore();

// 临时:打开文件夹 → scan → 控制台打印树(验证增量扫描算法迁移正确)。阶段 5 起替换为真实 UI。
async function scanAndPrint() {
  try {
    const result = await fsStore.openRoot();
    const root = result.folder;
    console.log('=== 扫描结果 ===');
    console.log(`根目录: ${root.name} | 文件 ${root.files.length} | 子文件夹 ${root.subFolders.length}`);
    console.log('新文件:', result.newFiles.map((f) => f.name));
    console.log('新子文件夹:', result.newSubFolders.map((f) => f.name));
    console.log('foldersData 缓存条目:', fsStore.foldersData.size);
    // 打印两层树结构
    for (const sub of root.subFolders) {
      console.log(`  📁 ${sub.name} (scanned=${sub.scanned}, files=${sub.files.length}, subs=${sub.subFolders.length})`);
    }
    console.log('全部媒体聚合:', fsStore.allMediaFolder.name);
  } catch (e) {
    console.error('扫描失败:', e);
  }
}
```

`<template>` 在 `.theme-switcher` 之后加：
```html
<div class="scan-zone">
  <button class="scan-btn" @click="scanAndPrint">📂 打开文件夹并扫描(F12 看控制台)</button>
  <span v-if="fsStore.currentFolder" class="scan-result">
    当前: {{ fsStore.currentFolder.name }} · {{ fsStore.currentFolder.files.length }} 文件
  </span>
</div>
```

`<style scoped>` 追加：
```css
.scan-zone { margin-top: 20px; display: flex; flex-direction: column; align-items: center; gap: 8px; }
.scan-btn {
  padding: 10px 20px; border: 2px dashed var(--color-primary, #3498db);
  border-radius: 8px; background: transparent; color: var(--color-primary, #3498db);
  cursor: pointer; font-size: 14px;
}
.scan-btn:hover { background: var(--color-primary, #3498db); color: #fff; }
.scan-result { color: var(--text-secondary, #666); font-size: 13px; }
```

- [ ] **Step 2: 验证 build 通过**

Run:
```bash
cd "d:/repos/相册浏览器-pwa" && npm run build 2>&1 | tail -8
```
Expected: 构建成功。

- [ ] **Step 3: Commit**

```bash
cd "d:/repos/相册浏览器-pwa" && rm -rf dist && git add src/App.vue && git commit -m "feat(阶段2): App.vue 临时 scan 按钮(打开文件夹→扫描→打印树)"
```

- [ ] **Step 4: 交付用户 dev 验收**

向用户报告阶段 2 完成情况 + 请 `npm run dev` 验收：
- 页面出现「📂 打开文件夹并扫描」按钮。
- 点击 → 浏览器弹出文件夹选择框 → 选一个含图片/视频的目录。
- **F12 控制台**打印：根目录名、文件数、子文件夹数、新文件名列表、新子文件夹列表、foldersData 缓存条目数、两层树结构、ALL_MEDIA 聚合节点。
- 页面按钮下方显示「当前: <目录名> · N 文件」。
- 控制台**无报错**（尤其关注 scan 的 import 链、appState 注入是否生效）。
- （可选）二次点按钮重选同一目录，验证增量扫描：`removedFileCount/newFiles` 应反映变化。

用户确认后 commit 阶段 2 里程碑：
```bash
cd "d:/repos/相册浏览器-pwa" && git commit --allow-empty -m "chore: 阶段 2(数据模型)完成,用户 dev 验收通过"
```

---

## Self-Review（已执行）

1. **Spec 覆盖**：spec 阶段 2（SmartFile/SmartFolder/TreeNode 去 global + 临时 scan 打印树）全覆盖。✓
2. **占位扫描**：SmartFile/TreeNode/fsStore/App.vue 代码完整；SmartFolder 标注「照搬源码 + 改造点清单」（scan 完整给出），执行时读源码逐字迁移。✓
3. **命名/类型一致**：`SmartFolder.appState`、`treeNodeRegistry`、`createAllMediaFolder`、`useFsStore.openRoot` 跨 task 一致；SmartFile.move 去 updateCount 与 TreeNode 退化（count 计算属性）一致。✓
4. **关键语义保留**：scan 增量算法（复用对象/Map 残留检测删除/single-pass）、syncChildren diff、blobUrl/md5 懒加载、getActualType 魔数均保留。✓
5. **副作用挪走**：ALL_MEDIA 模块加载副作用 → fsStore 初始化。✓
